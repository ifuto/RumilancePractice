Server branding assets
======================

server-icon.png
  1:1 server icon used for ops convenience (also copied to project root /
  build/libs after build when present).

Lunar Client Discord / ServerMappings listing icon
  Lunar Apollo rich presence and the Lunar ServerMappings listing do NOT
  pick this file up from the plugin JAR automatically. Upload the icon
  separately in Lunar's ServerMappings dashboard for the Discord game
  image to appear as "N ARENA".
