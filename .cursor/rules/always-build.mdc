---
description: コードを変えたら必ず test shadowJar と既存 Workflow の CI を確認して JAR を生成する
alwaysApply: true
---

# 毎回ビルドして JAR を出す

Java / Gradle / YAML / リソースを変更したら、そのターンの最後に必ず次を実行する。失敗したら直して再ビルドする。コンパイルや test だけで終わらせない。サーバーに置く fat JAR は `shadowJar` が出す。

```
./gradlew test shadowJar
```

完了後、`build/libs/RumilancePractice-<version>.jar` が今回のバージョンで存在することを確認する。`version` は `gradle.properties` が正。`build.gradle.kts` にバージョンを直書きしない。

ローカルだけで終わらせず、既存の `.github/workflows/build.yml` を最終コミットで毎回実行する。
失敗ログを確認して修正・再実行し、成功した run の `jars` Artifact と配布用 JAR を確認する。
完了報告には CI の結果と run リンク、JAR の入手先を記載する。詳しい手順・環境制約時の扱いは
リポジトリ直下の `AGENTS.md` に従う。
