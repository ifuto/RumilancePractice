# RumilancePractice プラグイン完全解説

> 対象バージョン: **1.1.0**
> このドキュメントは `src/` の全ソース・全リソースを読み、**省略なし**で整理したものです。

---

## 変更履歴

### v1.1.0
- `/profile` をチャット表示から**専用 GUI（ProfileGui）**に変更（キット別詳細付き・ミニマル配色）
- スコアボードを**ミニマル構成**に刷新（鯖名 + IP + Online/Queue、試合中は Kit/Mode/スコア/Time）
- デュエルに**チームカラー（RED / BLUE）**を付与（重複なし、再戦チェーン中は固定）
- スコアボードに**連戦スコア（0-0 → 1-2 …）**を表示。加算は「再戦ボタンによる連続対戦」のみ
- `/ranked`（`/rd`）を `/duel` の別名として追加
- `/kdr <kit> [player]` → **`/kdr [player] [kit]`** に変更（kit 省略時は全体 K/D）
- 全 GUI タイトル・機能アイテムから**太字（BOLD）を除去**、配色を白/グレー中心に整理
- `config.yml` に `scoreboard.server-name` / `scoreboard.server-ip` を追加

---

## 目次

1. [概要](#1-概要)
2. [動作環境・技術スタック](#2-動作環境技術スタック)
3. [ビルドと導入](#3-ビルドと導入)
4. [ファイル構成](#4-ファイル構成)
5. [コマンド一覧](#5-コマンド一覧)
6. [権限一覧](#6-権限一覧)
7. [設定ファイル詳細](#7-設定ファイル詳細)
8. [データベース](#8-データベース)
9. [起動フロー](#9-起動フロー)
10. [コアシステム解説](#10-コアシステム解説)
11. [GUI 一覧](#11-gui-一覧)
12. [アイテムタグ（PDC キー）一覧](#12-アイテムタグpdc-キー一覧)
13. [リスナー一覧](#13-リスナー一覧)
14. [データモデル一覧](#14-データモデル一覧)
15. [ユーティリティ一覧](#15-ユーティリティ一覧)
16. [テスト一覧](#16-テスト一覧)
17. [管理者セットアップ手順](#17-管理者セットアップ手順)
18. [トラブルシューティング・注意点](#18-トラブルシューティング注意点)

---

## 1. 概要

**RumilancePractice** は Paper 1.21.11 向けの **Practice PvP（練習サーバー）プラグイン**です。

- ランク戦 / アンランク戦の **1v1 デュエル**（キュー + 直接申請）
- **FFA** アリーナ
- **ロビー** システム（スポーン・保護・インベントリ・機能アイテム）
- **キット** システム（公式キット + 個人レイアウト編集 `/ekit` + オリジナルキット）
- **FAWE / WorldEdit** によるアリーナ再生成（未導入でも動作）
- Elo レーティング・ランキング・統計・プロフィール
- 観戦・スコアボード・多言語化・ChatBan など

**重要仕様（設計上の要点）**

| 仕様 | 内容 |
|---|---|
| アンランク戦は統計を一切変更しない | `UnrankedResultProcessor` は監査ログ（audit_log）に記録するのみ。Elo / 勝率 / K/D / 公開統計は変更しない |
| GUI の識別 | タイトル文字列ではなく `PracticeGuiHolder`（セッションUUID + GuiType）で識別 |
| 機能アイテムの識別 | 表示名ではなく PDC `function_type` で識別 |
| プレイヤー状態 | UUID キーの `PlayerStateManager` で一意管理（状態機械 + 遷移検証） |
| サーバー停止時の ChatBan | `MatchSession.shuttingDown` 中は切断 ChatBan を発行しない |

---

## 2. 動作環境・技術スタック

| 項目 | 内容 |
|---|---|
| サーバー | **Paper 1.21.11**（`api-version: '1.21'`、Folia 非対応） |
| Java | **21+**（Gradle toolchain 21、`options.release=21`） |
| ソフト依存 | **FastAsyncWorldEdit / WorldEdit**（推奨。未導入時はアリーナ再生成を無効化して警告を出して起動） |
| ビルド | Gradle（Shadow 9.6.0、configuration cache 有効） |
| 同梱ライブラリ（shaded + リロケート） | HikariCP 7.1.0 / sqlite-jdbc 3.53.2.0 / mariadb-java-client 3.5.9 / slf4j |
| リロケート先 | `com.rumilance.practice.libs.*` |
| その他依存（compileOnly） | `io.papermc.paper:paper-api:1.21.11-R0.1-SNAPSHOT`、`com.sk89q.worldedit:worldedit-bukkit:7.3.0` |
| テスト | JUnit 5（junit-bom 6.1.2） |

**ビルド成果物（2 種類）**

| ファイル | 種別 | 用途 |
|---|---|---|
| `build/libs/RumilancePractice-1.1.0.jar` | **shadowJar（正）** | サーバーに入れるのはこちら（DB ドライバ同梱） |
| `build/libs/RumilancePractice-1.1.0-thin.jar` | thin jar（`tasks.jar`） | 依存なし。単体では DB 初期化に失敗するため**非推奨** |

CI（GitHub Actions `.github/workflows/build.yml`）: Java 21（Temurin）+ `./gradlew build` を実行し、`build/libs/*.jar` をアーティファクト `jars` にアップロード。

---

## 3. ビルドと導入

```bash
cd RumilancePractice
./gradlew build          # Windows は ./gradlew.bat build
```

成果物: `build/libs/RumilancePractice-1.1.0.jar`

導入:

1. `plugins/` に `RumilancePractice-1.1.0.jar` を配置（**thin ではなく通常版**）
2. （推奨）FAWE / WorldEdit を導入
3. サーバー起動 → 初回起動時に `plugins/RumilancePractice/` へ各 YAML が生成される
4. 管理者セットアップを実施（[§17](#17-管理者セットアップ手順)）

> 初回起動時に `config.yml` 等が無ければ jar 内のデフォルトがコピーされ、既存ファイルは jar 内デフォルトを `setDefaults` として重ねるため、一部だけ書き換えた設定でも新キーが失われません（`ConfigService.loadWithDefaults`）。

---

## 4. ファイル構成

### 4.1 ルート

| パス | 内容 |
|---|---|
| `README.md` | 概要・セットアップ手順 |
| `SOUNDS.md` | 効果音リファレンス（1.21） |
| `build.gradle.kts` | ビルド定義（version 1.1.0） |
| `settings.gradle.kts` / `gradle.properties` | Gradle 設定（並列ビルド、キャッシュ、configuration-cache） |
| `gradlew` / `gradlew.bat` / `gradle/wrapper/` | Gradle wrapper（9.6.1） |
| `.github/workflows/build.yml` | CI（ビルド + jars アップロード） |
| `.gitignore` | `.gradle/` `build/` `out/` 等 |

### 4.2 ソース（`src/main/java/com/rumilance/practice/`）

| パッケージ | ファイル | 役割 |
|---|---|---|
| （ルート） | `RumilancePractice.java` | プラグイン主クラス（onEnable / onDisable） |
| `bootstrap` | `FeatureBootstrap.java` | 全サービスの生成・登録・コマンド結線・リスナー登録 |
| `bootstrap` | `ServiceRegistry.java` | DI コンテナ（型 → インスタンス） |
| `config` | `ConfigService.java` / `PluginSettings.java` / `RuntimeFlags.java` | YAML 読み書き / 型付き設定スナップショット / 実行時フラグ |
| `database` | `DatabaseService.java` / `DatabaseType.java` / `SchemaMigrator.java` | HikariCP プール / SQLITE・MARIADB / バージョン付きマイグレーション |
| `database.repository` | `PlayerRepository` / `SettingsRepository` / `RankedStatsRepository` / `MatchHistoryRepository` / `PunishmentRepository` / `AuditLogRepository` / `KitLayoutRepository` / `OriginalKitRepository` / `DailyRankedStatsRepository` / `FfaStatsRepository` / `ObjectionRepository` | テーブルごとの永続化 |
| `session` | `SessionManager` / `PlayerSession` / `MatchSession` / `PlayerStateManager` / `IllegalStateTransitionException` | セッション管理と状態機械 |
| `state` | `PlayerState` / `MatchState` / `MatchMode` / `ArenaType` / `ArenaTerrain` / `ArenaInstanceState` | 状態・種別 enum |
| `lobby` | `LobbyService` / `LobbyListener` | ロビー座標・領域・保護 |
| `kit` | `KitService` / `KitLayoutCache` | キット定義の読み込み・適用・個人レイアウトキャッシュ |
| `queue` | `QueueService` / `QueueCoordinator` | マッチメイキング |
| `duel` | `DuelRequestService` | デュエル申請（TTL・レート制限） |
| `match` | `MatchService` / `MatchRegistry` / `MatchListener` / `ArenaBoundsListener` | 試合ライフサイクル・戦闘ルール |
| `match.result` | `MatchResultProcessor` / `RankedResultProcessor` / `UnrankedResultProcessor` / `FfaResultProcessor` | 結果処理（ランク / アンランク / FFA） |
| `arena` | `ArenaService` / `AbstractArenaService` / `SimpleArenaService` / `FaweArenaService` / `ArenaTemplateStore` | アリーナ予約・再生成 |
| `arena.fawe` | `FaweBridge` / `FaweBridgeImpl` / `NoOpFaweBridge` | FAWE/WE 抽象化 |
| `ffa` | `FfaService` / `FfaListener` | FFA アリーナ・戦闘ループ |
| `spectator` | `SpectatorService` | 観戦 |
| `settings` | `SettingsService` | プレイヤー設定のキャッシュ + 非同期保存 |
| `sound` | `SoundService` | 設定駆動サウンド |
| `locale` | `LocaleService` / `MessageService` | 多言語化（MiniMessage） |
| `stats` | `StatsService` | 統計・プロフィール・ランキング取得 |
| `scoreboard` | `ScoreboardService` | スコアボード / TAB |
| `punishment` | `ChatBanService` | ChatBan / 異議申し立て |
| `arrow` | `ArrowEffectService` | 矢の軌跡エフェクト |
| `originalkit` | `OriginalKitService` / `ClientModBridge` | オリジナルキット / クライアント Mod ブリッジ（NoOp） |
| `gui` | `AbstractGui` / `GuiDecorator` / `GuiListener` / `GuiSession` / `GuiSessionRegistry` / `GuiType` / `PracticeGuiHolder` | GUI 基盤 |
| `gui.menus` | 14 個の GUI（後述） | 各種メニュー |
| `admin` | `AdminTools` / `AdminToolListener` | 管理者ツール |
| `item` | `FunctionalItemListener` | ロビー機能アイテム |
| `listener` | `SessionBootstrapListener` / `PracticeSideListener` | 参加時セットアップ / チャット・コマンド・矢監視 |
| `command` | 12 ファイル | コマンド実装 |
| `util` | `AsyncExecutor` / `Cuboid` / `GuiSlots` / `ItemKeys` / `ItemSerializer` / `LocationUtil` | 共通ユーティリティ |
| `elo` | `EloCalculator` / `EloRating` / `EloUpdateResult` / `MatchOutcome` | Elo 計算 |
| `model` | 12 個のレコード | データモデル |

### 4.3 リソース（`src/main/resources/`）

| ファイル | 内容 |
|---|---|
| `plugin.yml` | プラグイン定義・全コマンド・全権限 |
| `config.yml` | メイン設定 |
| `database.yml` | DB 接続設定 |
| `kits.yml` | デフォルトキット（nodebuff / boxing / sumo / gapple） |
| `arenas.yml` | サンプルアリーナ（flat_1 / flat_2） |
| `ffa.yml` | FFA 設定（アリーナ無し初期値） |
| `lobby.yml` | ロビースポーン・領域・保護 |
| `gui.yml` | GUI レイアウト |
| `sounds.yml` | 効果音定義 |
| `arrow-effects.yml` | 矢エフェクト定義 |
| `plans.yml` | プラン制限 |
| `profile.yml` | プロフィールデフォルト |
| `lang/en_us.yml` / `ja_jp.yml` / `en_gb.yml` / `ko_kr.yml` | 翻訳（MiniMessage） |

---

## 5. コマンド一覧

`plugin.yml` に定義された**全 33 コマンド**（FeatureBootstrap が全てエグゼキュータに結線済み。結線されないと `Missing command in plugin.yml` 警告が出る設計）。

### 5.1 プレイヤーコマンド

| コマンド | エイリアス | 内容 | 権限 |
|---|---|---|---|
| `/duel` | `d` | ランク戦キュー GUI。`/duel <player>` で直接申請、`/duel cancel\|accept\|deny` も可 | rumilance.user |
| `/ranked` | `rd` | `/duel` と同機能（ランク戦キュー GUI / 申請） | rumilance.user |
| `/unranked` | `unrankduel`, `unduel`, `ud` | アンランク戦キュー GUI / 申請 | rumilance.user |
| `/accept [player]` | – | デュエル申請を承認（省略時は最新の受信申請） | rumilance.user |
| `/deny [player\|all]` | – | 申請を拒否（`all` で全拒否） | rumilance.user |
| `/queue leave` | – | キュー離脱 | rumilance.user |
| `/players` | `dplayers` | オンラインプレイヤー一覧 → デュエル申請 GUI | rumilance.user |
| `/spec <player>` | – | プレイヤーの試合を観戦 | rumilance.user |
| `/specgui` | – | 進行中マッチ一覧 → 観戦 | rumilance.user |
| `/setting` | – | 個人設定 GUI | rumilance.user |
| `/stats [player]` | – | ランク戦キット統計 GUI | rumilance.user |
| `/kdr [player] [kit]` | – | 指定プレイヤーのキット別 or 全体 K/D（kit 省略時は全キット合算） | rumilance.user |
| `/profile [player]` | – | ランク戦プロフィール（総試合・勝敗・勝率・K/D・最高連勝・得意キット） | rumilance.user |
| `/ranking <elo\|kill\|matches\|winstreak>` | – | ランキング Top10 表示 | rumilance.user |
| `/ffa` | – | FFA 一覧 GUI / 管理サブコマンド | rumilance.user（管理サブコマンドは admin） |
| `/ekit` | – | 公式キット個人レイアウト編集 GUI | rumilance.user |
| `/leave` | `le` | カウントダウン中に試合を抜ける（連続3回で3日 ChatBan） | rumilance.user |
| `/lobby` | `spawn` | ロビーへ帰還（観戦中・FFA 中は離脱） | rumilance.user |
| `/spawn` | – | 同上 | rumilance.user |
| `/ping [player]` | – | Ping 表示 | rumilance.user |
| `/matchhistory [player]` | – | 直近10試合のランク戦履歴 | rumilance.user |
| `/objection <reason>` | – | ChatBan への異議申し立て | rumilance.user |
| `/arroweffect` | – | 矢エフェクト選択 GUI | **rumilance.user.mem**（mem/vip/vip_plus/OP） |
| `/originalkit` | `okit` | オリジナルキット編集 GUI | rumilance.user |

### 5.2 管理者コマンド

| コマンド | 内容 | 権限 |
|---|---|---|
| `/practiceadmin tool` | Region Selector（ブレイズロッド）と Setup Menu（ネザースター）を配布 | rumilance.admin |
| `/practiceadmin reload` | kits / arenas / ffa / lobby / sounds を安全に再読み込み | rumilance.admin |
| `/practiceadmin status` | アクティブマッチ数・メンテナンス状態表示 | rumilance.admin |
| `/practiceadmin matches` | 全マッチ一覧（id / mode / state / kit） | rumilance.admin |
| `/practiceadmin cleanup` | 強制マッチクリーンアップ | rumilance.admin |
| `/practiceadmin maintenance <on\|off>` | メンテナンスモード切替（config.yml にも保存） | rumilance.admin |
| `/slobby pos1` / `pos2` | ロビー領域の角を設定 | rumilance.admin |
| `/slobby spawn` | ロビースポーン設定（ワールドボーダー外なら警告） | rumilance.admin |
| `/slobby info` | スポーン・領域表示 | rumilance.admin |
| `/slobby validate` | ロビー設定の検証（領域内 / ボーダー内か等） | rumilance.admin |
| `/setlobbyitem` | 現在の手持ちインベントリをロビーインベントリとして保存 | rumilance.admin |
| `/setfunc <ranked\|unranked\|ffa\|ekit\|settings\|spectate\|leavequeue>` | 手持ちアイテムを機能アイテム化（素手ならネザースター） | rumilance.admin |
| `/arena <subcommand>` | アリーナ管理（下記） | rumilance.admin |
| `/kit [subcommand]` | キット管理（引数なしで管理 GUI） | rumilance.admin |
| `/toggle <queue\|map> <enable\|disable> <id>` | キュー / マップの有効無効 | rumilance.admin |
| `/chatban <player> <duration> <reason>` | 手動 ChatBan | rumilance.admin |
| `/chatunban <player\|chatban_id>` | ChatBan 解除 | rumilance.admin |

#### `/arena` サブコマンド

`draft <name>` / `selection apply <draft>` / `p1 <draft>` / `p2 <draft>` / `type <ANY|FLAT|BUMPY|CRYSTAL|NETHERITE> <draft>` / `save <draft>`（FAWE 有効時は `.schem` 保存。保存後は disabled で登録）/ `enable|disable <name>` / `list` / `info <name>` / `delete <name>`

#### `/kit` サブコマンド

`gui` / `help` / `create <name>`（現在の装備から作成）/ `overwrite <name>` / `list` / `info <name>` / `enable|disable <name>` / `delete <name>` / `timeout <name> <sec>` / `adventure <name> <on|off>` / `autoregen|autofood|blockplace|blockbreak|canbreak|pearl|totem|swordshieldbreak <name> <on|off>`

#### `/ffa` サブコマンド（管理）

`create <arena>`（pos1/pos2 使用、無効状態で作成）/ `selection apply <arena>` / `spawn <arena>` / `kit <arena> <kit>` / `enable|disable|delete|reset <arena>` / `leave`（プレイヤー用）

---

## 6. 権限一覧

| 権限 | 説明 | デフォルト |
|---|---|---|
| `rumilance.user` | 基本プレイヤー権限 | **true** |
| `rumilance.duel.use` | デュエル / キューコマンド | **true** |
| `rumilance.admin` | 全管理（children: user / lobby.bypass / punishment.bypass） | **op** |
| `rumilance.lobby.bypass` | ロビー領域制限の回避（ブロック破壊・設置許可等） | op |
| `rumilance.punishment.bypass` | 切断 ChatBan の回避 | op |
| `rumilance.user.def` | デフォルトプラン | true |
| `rumilance.user.mem` | メンバープラン（矢エフェクト利用可） | false |
| `rumilance.user.vip` | VIP プラン | false |
| `rumilance.user.vip_plus` | VIP+ プラン | false |

---

## 7. 設定ファイル詳細

### 7.1 `plugin.yml`

```yaml
name: RumilancePractice
main: com.rumilance.practice.RumilancePractice
version: ${version}        # ビルド時に 1.1.0 へ展開（processResources）
api-version: '1.21'
folia-supported: false
softdepend: [FastAsyncWorldEdit, WorldEdit]
```

### 7.2 `config.yml`

| キー | デフォルト | 内容 |
|---|---|---|
| `plugin.default-locale` | `en_us` | 未選択時・フォールバック言語 |
| `plugin.debug` | `false` | 詳細デバッグログ |
| `plugin.maintenance` | `false` | メンテナンス（`/practiceadmin maintenance` で切替） |
| `executor.min-threads` | `1` | 非同期ワーカー最小（1〜4） |
| `executor.max-threads` | `4` | 非同期ワーカー最大（1〜4） |
| `executor.core-threads` | `2` | コアスレッド数 |
| `executor.keep-alive-seconds` | `60` | アイドルスレッド生存時間 |
| `match.countdown-seconds` | `5` | 試合開始カウントダウン |
| `match.max-duration-seconds` | `0` | 最大試合時間（0=無制限） |
| `match.reconnect-grace-seconds` | `30` | 切断後の再接続猶予 |
| `match.regenerate-arena` | `true` | FAWE によるアリーナ再生成 |
| `match.end-teleport-delay-seconds` | `5` | 終了後のロビー帰還遅延 |
| `queue.max-wait-notify-seconds` | `60` | 待機通知までの時間 |
| `queue.initial-elo-range` | `75` | 初期 Elo マッチング幅 |
| `queue.elo-range-growth-per-interval` | `25` | 待機に応じた幅拡大 |
| `queue.growth-interval-seconds` | `15` | 拡大インターバル |
| `ranking.starting-elo` | `1000` | 初期 Elo |
| `ranking.k-factor.provisional-games` | `20` | 暫定試合数 |
| `ranking.k-factor.provisional-k` | `64` | 暫定 K 値 |
| `ranking.k-factor.standard-k` | `32` | 標準 K 値 |
| `ranking.k-factor.top-percent-fraction` | `0.10` | 上位10%判定 |
| `ranking.k-factor.top-percent-k` | `26` | 上位10%の K 値 |
| `spectator.allow-flight` | `true` | 観戦者の飛行 |
| `spectator.hide-from-players` | `true` | 観戦者を他プレイヤーから隠す |
| `lobby.world` | `world` | ロビーワールド |
| `lobby.return-to-lobby-on-death` | `false` | 死亡時ロビー帰還 |
| `fawe.enabled` | `true` | FAWE 使用試行 |
| `fawe.operation-timeout-seconds` | `30` | 再生成タイムアウト |
| `hooks.placeholderapi` | `true` | PlaceholderAPI フック（現状未実装フラグ） |
| `scoreboard.enabled` | `true` | スコアボード |
| `scoreboard.update-interval-ticks` | `20` | 更新間隔（10〜20 にクランプ） |
| `scoreboard.tab-header-footer` | `true` | TAB ヘッダー/フッター |
| `scoreboard.server-name` | `Rumilance` | サイドバー先頭・TAB ヘッダーに表示する鯖名 |
| `scoreboard.server-ip` | `play.example.com` | サイドバー・TAB フッターに表示する鯖 IP/アドレス |
| `performance.particle-limit-per-player` | `40` | プレイヤー毎のパーティクル上限 |

### 7.3 `database.yml`

| キー | デフォルト | 内容 |
|---|---|---|
| `storage.type` | `SQLITE` | `SQLITE` / `MARIADB`（`MYSQL` は MARIADB の別名） |
| `storage.sqlite.file` | `data/rumilance-practice.db` | データフォルダ相対パス |
| `storage.mariadb.*` | – | host / port / database / username / password / use-ssl / parameters |
| `pool.maximum-pool-size` | `10` | 最大プール（SQLite は強制 1） |
| `pool.minimum-idle` | `2` | 最小アイドル（SQLite は強制 1） |
| `pool.connection-timeout-ms` | `10000` | 接続タイムアウト |
| `pool.idle-timeout-ms` | `600000` | アイドルタイムアウト |
| `pool.max-lifetime-ms` | `1800000` | 最大生存時間 |
| `pool.leak-detection-threshold-ms` | `0` | リーク検出（0=無効） |
| `pool.pool-name` | `RumilancePracticePool` | プール名 |
| `schema.auto-migrate` | `true` | 自動マイグレーション |
| `schema.table-prefix` | `rp_` | テーブルプレフィックス |

### 7.4 `kits.yml`（デフォルト4キット）

各キットの項目（すべて省略可）:

| キー | デフォルト | 内容 |
|---|---|---|
| `display-name` | キット名 | MiniMessage 表示名 |
| `icon` | `DIAMOND_SWORD` | GUI アイコン素材 |
| `ranked` | `true` | ランク戦対象 |
| `ffa-enabled` | `true` | FFA 利用可 |
| `max-health` | `20.0` | 最大 HP |
| `natural-health-regen` | `true` | 自然回復（= `/kit autoregen`） |
| `knockback-multiplier` | `1.0` | ノックバック倍率 |
| `enabled` | `true` | 有効 |
| `arena-terrain` | `ANY` | 使用アリーナ地形 |
| `auto-food` | `false` | 自動食料回復 |
| `sword-shield-break` | `false` | 剣での盾破壊 |
| `block-place` / `block-break` | `false` | ブロック設置 / 破壊 |
| `can-break` | `[]` | block-break 無効時でも破壊できる素材リスト |
| `pearl` | `true` | エンダーパール |
| `totem` | `true` | 不死のトーテム |
| `adventure` | `false` | アドベンチャーモード強制 |
| `timeout-seconds` | `0` | 試合時間制限（0=設定なし） |
| `items` | `[]` | スロット・素材・個数 |
| `armor` | `{}` | helmet / chestplate / leggings / boots |

デフォルトキット:

| キット | 表示名 | アイコン | 特徴 |
|---|---|---|---|
| `nodebuff` | `<gray>NoDebuff` | IRON_SWORD | 剣+弓+矢64（ranked / ffa 可） |
| `boxing` | `<yellow>Boxing` | LEATHER_HELMET | レザー防具一式、アイテム無し |
| `sumo` | `<aqua>Sumo` | SLIME_BALL | ノックバック 1.5 倍、FFA 不可 |
| `gapple` | `<green>Gapple` | GOLDEN_APPLE | ダイヤ剣+金リンゴ16+ダイヤ防具 |

### 7.5 `arenas.yml`（サンプル2つ）

- `flat_1` / `flat_2`: タイプ `DUEL`、地形 `FLAT`、ワールド `world`、enabled: `true`、schematic 空
- 各テンプレートのキー: `id`（UUID）/ `type`（DUEL|FFA）/ `terrain`（ANY|FLAT|BUMPY|CRYSTAL|NETHERITE）/ `world` / `enabled` / `min` / `max`（領域）/ `spawn-a` / `spawn-b`（x,y,z,yaw,pitch）/ `schematic`
- 複数の enabled な DUEL テンプレートがあると並行マッチ / 再戦が可能（アリーナ1テンプレート=1インスタンスのため）

### 7.6 `ffa.yml`

- `arenas: []`（初期値）
- `settings.min-players-to-start: 1` / `max-players-per-arena: 24` / `respawn-delay-seconds: 3` / `kill-streak-announcements: true` / `environment-damage-enabled: false`

### 7.7 `lobby.yml`

- `spawn`: world / x / y / z / yaw / pitch（`/slobby spawn` で更新）
- `fall-return-y: 0.0`
- `region`（コメントアウト初期値）: world / pos1 / pos2（`/slobby pos1/pos2` で保存）
- `restrictions`: disable-pvp / disable-hunger / disable-item-drop / disable-item-pickup / disable-block-break / disable-block-place
- `inventory-base64`: `/setlobbyitem` で保存されるロビーインベントリ

### 7.8 `gui.yml`

| セクション | 内容 |
|---|---|
| `main-menu` | タイトル・27スロット・各ボタンスロット・フィラー |
| `kit-select` | 54スロット、キット10〜43、戻る49 |
| `ffa-select` | 27スロット、アリーナ10〜16、戻る22 |
| `settings-menu` | 45スロット（locale 11 / sounds 13 / scoreboard 15 / arrow-effect 20 / spectate-visibility 22 / 戻る40） |
| `spectator-menu` | 27スロット（A 11 / B 15 / leave 22） |
| `confirm-dialog` | 27スロット（confirm 11 / cancel 15） |

> ※ 実際のコードでは GUI レイアウトは各 `*Gui.java` 内にハードコードされており、`gui.yml` は現時点で参照されていない（将来用のテンプレート）。

### 7.9 `sounds.yml`（全イベント）

`select` / `queue-leave` / `gui-back`（none）/ `gui-open`（none）/ `kit-select` / `gui-click` / `duel-request-received` / `duel-request-sent` / `queue-joined` / `cancel` / `delete` / `match-found` / `death` / `match-start` / `match-end-anvil` / `match-end-levelup` / `match-countdown-tick` / `error`

- 各定義: `key`（NamespacedKey 形式、`none` で無音）/ `volume` / `pitch`
- 解決失敗時は `ui.button.click` にフォールバック（`SoundService.resolve`）

### 7.10 `arrow-effects.yml`

- `effects`: none（particle null）/ flame（FLAME）/ crit（CRIT）/ soul（SOUL_FIRE_FLAME）/ heart（HEART）/ witch（WITCH）
- 各: `display-name` / `particle` / `particles-per-tick`
- `default-effect: none` / `unlock-all-by-default: false`（※実際のロック判定は権限ベース）

### 7.11 `plans.yml`

- `plans: {}`
- `original-kit.monthly-edits`: default 10 / member 30 / vip 100 / vip_plus -1（無制限）
- `defaults`: warmup-minutes 5 / session-minutes 45 / cooldown-minutes 5 / notify-before-seconds 30

### 7.12 `profile.yml`

- `defaults`: locale null（= プラグイン既定）/ sounds-enabled true / scoreboard-enabled true / arrow-effect none / spectate-visible true / accept-duel-requests true / show-ranked-prompt true
- `scoreboard`: enabled true / update-interval-ticks 20 / title
- `statistics`: track-unranked / track-ranked / track-block-and-item-usage（現状は未使用フラグ）
- プロフィール表示の `progress.char` / `progress.length` はコードで参照

### 7.13 `lang/*.yml`

- 同梱: `en_us`（デフォルト）/ `ja_jp` / `en_gb` / `ko_kr`
- MiniMessage 形式。`plugins/RumilancePractice/lang/<locale>.yml` に置くと jar 内より優先
- キー解決順: プレイヤーロケール → デフォルトロケール → `!key!` マーカー
- キー一覧（抜粋）: `general.*`（maintenance / no-permission / player-only / player-not-found / invalid-usage / reload-success / **unknown-error**（「予期しないエラーが発生しました。管理者に連絡してください。」— ※現在 Java コードから未参照）/ feature-disabled / loading / mode-ranked / mode-unranked）ほか `duel.*` `duel-gui.*` `ffa.*` `kit.*` `originalkit.*` `arena.*` `lobby.*` `ranking.*` `queue.*` `match.*` `spectator.*` `settings.*` `stats.*` `admin.*` `punishment.*` `plan.*` `arrow.*` `admin-gui.*`

---

## 8. データベース

### 8.1 バックエンド

- `DatabaseService` が HikariCP プールを生成。SQLite（デフォルト、`data/rumilance-practice.db`）または MariaDB/MySQL
- 全テーブルは `rp_` プレフィックス、UUID は `CHAR(36)`、SQLite / MariaDB 両対応の ANSI 系 SQL
- `SchemaMigrator` が `rp_schema_version` テーブルを見てバージョン付きで DDL を適用（現行 v12）
- upsert: SQLite は `ON CONFLICT ... DO UPDATE`、MariaDB は `ON DUPLICATE KEY UPDATE`

### 8.2 テーブル一覧

| テーブル | マイグレーション | 主キー / 制約 | 主なカラム |
|---|---|---|---|
| `rp_schema_version` | – | version | version, applied_at |
| `rp_players` | v1 | uuid | username, first_join, last_seen, locale |
| `rp_player_settings` | v2 / **v9 拡張** | uuid | sounds_enabled, scoreboard_enabled, arrow_effect, spectate_visible, accept_duel_requests, locale, **auto_requeue, hide_other_chat, chat_whitelist** |
| `rp_ranked_stats` | v3 | id / UNIQUE(uuid, kit) | elo, wins, losses, win_streak, best_elo |
| `rp_match_history` | v4 | id | player_a, player_b, kit, mode, winner, ranked, started_at, ended_at |
| `rp_punishments` | v5 | id | target_uuid, staff_uuid, type, reason, issued_at, expires_at, revoked |
| `rp_audit_log` | v6 | id | actor_uuid, action, details, created_at |
| `rp_kit_layouts` | v7 | id / UNIQUE(uuid, kit) | item_data(TEXT) |
| `rp_original_kits` | v8 | uuid | item_data, armor_data, saved_at |
| `rp_daily_ranked_stats` | v10 | PK(player_uuid, stat_date) | kills, matches |
| `rp_objections` | v11 | id | chatban_id, player_uuid, reason, status, created_at, staff_uuid, staff_note |
| `rp_ffa_stats` | v12 | PK(player_uuid, arena_id) | kills, deaths |

### 8.3 リポジトリ

`PlayerRepository`（findByUuid / upsert / updateLastSeen / findAll）、`SettingsRepository`（findByUuid / findOrDefault / upsert）、`RankedStatsRepository`（find / upsert / topByKit / findAllForPlayer / findAllOrderedByWinStreak / findTopEloOverall）、`MatchHistoryRepository`（insert / findById / findRecentForPlayer）、`PunishmentRepository`（insert / revoke / findById / findActiveForPlayer / findHistoryForPlayer）、`AuditLogRepository`（insert / findRecent）、`KitLayoutRepository`（find / upsert / delete / findAllForPlayer）、`OriginalKitRepository`（upsert / find / delete）、`DailyRankedStatsRepository`（increment / topKillsToday / topMatchesToday）、`FfaStatsRepository`（find / addKill / addDeath）、`ObjectionRepository`（insert / findById / findAll）

---

## 9. 起動フロー

`RumilancePractice.onEnable()`:

1. `ServiceRegistry` 生成、`ItemKeys.init(this)`
2. `ConfigService.loadAll()`（11 ファイル）→ `PluginSettings.from(config)` スナップショット
3. `AsyncExecutor`（1〜4 スレッド）生成
4. `SessionManager` / `PlayerStateManager` 生成
5. `LocaleService`（`plugins/RumilancePractice/lang` も参照）+ `MessageService`
6. **`initializeDatabase()`**: `DatabaseService` → `SchemaMigrator.migrate()` → 全リポジトリ登録。**失敗時は SEVERE ログの上でプラグイン無効化**
7. `EloCalculator` 生成
8. `FaweBridge` 選択（FAWE/WE 検出 → `FaweBridgeImpl`、無ければ `NoOpFaweBridge` + 警告ログ）
9. `FeatureBootstrap.enable()` → 全サービス・GUI・リスナー・コマンド結線 → 「RumilancePractice has been enabled.」

`onDisable()`: FeatureBootstrap.disable → DB close → AsyncExecutor shutdown(10s) → ServiceRegistry クリア。

`FeatureBootstrap.enable()` の結線順（重要）:

1. `RuntimeFlags`（メンテナンス）登録
2. `SettingsService` → `SoundService` → `LobbyService` → `KitService` → `KitLayoutCache` → `ArenaTemplateStore.reload()` → `ArenaService`（FAWE 有無で切替）→ `GuiSessionRegistry` → `QueueService` → `DuelRequestService`（TTL 60s、レート制限 3s）
3. `MatchRegistry` + 結果プロセッサ（Ranked / Unranked / FFA）→ `MatchService` → `ChatBanService`（既定 7日）→ `SpectatorService` → `FfaService` → `StatsService` → `ArrowEffectService.start()` → `QueueCoordinator.start()`（40tick マッチング / 20tick アクションバー）
4. 全 GUI 生成・`GuiListener` 登録 → `FunctionalItemListener`（開く GUI をセット）
5. `ScoreboardService.start()`（設定が有効なら）
6. リスナー登録: `SessionBootstrapListener` / `LobbyListener` / `MatchListener` / `ArenaBoundsListener` / `FfaListener` / `GuiListener` / `FunctionalItemListener` / `AdminToolListener` / `PracticeSideListener`
7. コマンド結線（`bind()`）: 上記 [§5](#5-コマンド一覧) の通り。`originalkit` はラムダで直接結線

---

## 10. コアシステム解説

### 10.1 プレイヤー状態機械（`PlayerStateManager` / `PlayerState`）

状態一覧: `IDLE` → `LOBBY` → `OPENING_GUI` / `QUEUED_RANKED` / `QUEUED_UNRANKED` / `REQUESTING_DUEL` / `SPECTATING` / `FFA` / `EDITING_KIT` → `PREPARING_MATCH` → `COUNTDOWN` → `FIGHTING` → `ENDING`

許可される遷移（遷移表）:

```
IDLE            -> LOBBY
LOBBY           -> OPENING_GUI, QUEUED_RANKED, QUEUED_UNRANKED, REQUESTING_DUEL, SPECTATING, FFA, EDITING_KIT
OPENING_GUI     -> LOBBY, QUEUED_RANKED, QUEUED_UNRANKED, REQUESTING_DUEL, SPECTATING, FFA, EDITING_KIT
QUEUED_RANKED   -> LOBBY, PREPARING_MATCH
QUEUED_UNRANKED -> LOBBY, PREPARING_MATCH
REQUESTING_DUEL -> LOBBY, PREPARING_MATCH
PREPARING_MATCH -> COUNTDOWN, LOBBY
COUNTDOWN       -> FIGHTING, LOBBY
FIGHTING        -> ENDING
ENDING          -> LOBBY, PREPARING_MATCH
SPECTATING      -> LOBBY
FFA             -> LOBBY
EDITING_KIT     -> LOBBY
```

- 不正遷移は `IllegalStateTransitionException` を投げる
- `resetToLobby()` は無条件に LOBBY へ（キュークラッシュ / 管理クリーンアップ / リロード時の安全網）
- 仕様上のバグ防止: 二重キュー不可、FIGHTING 中の申請承認不可、クラッシュ後の復帰は強制リセット

### 10.2 セッション（`SessionManager` / `PlayerSession` / `MatchSession`）

- `PlayerSession`: オンライン中のプレイヤーの実行時状態（locale / selectedKit / arrowEffect / currentMatchId / soundsEnabled / scoreboardEnabled）
- `MatchSession`: 1 試合の実行時表現。id / mode / kitName / participants / terrain / bestOf / arenaInstanceId / kills / roundWins / winner / draw / state / rematchRequestedA・B / shuttingDown
  - 冪等ガード: `tryMarkResultApplied()`（結果二重適用防止）、`tryMarkDisconnectPenalty()`（切断ペナルティ一回のみ）

### 10.3 ロビー（`LobbyService` / `LobbyListener`）

- `sendToLobby`: アドベンチャーモード、HP/食料回復、火・落下リセット、飛行解除、エフェクト除去、ロビーインベントリ適用、スポーンへ `teleportAsync`（WorldBorder セーフ位置へクランプ）
- `applyLobbyInventory`: 保存された 41 スロット（36 インベントリ + 4 防具 + オフハンド）を適用
- `setSpawn` / `setRegion` / `saveLobbyInventory` / `isConfigured` / `validate`（スポーン・領域・包含・ボーダー検査）
- `LobbyListener`: LOBBY / OPENING_GUI / QUEUED_* / REQUESTING_DUEL / IDLE 状態のプレイヤーを保護
  - ダメージ（落下・近接・投射物）キャンセル、空腹固定、ブロック破壊/設置キャンセル（`rumilance.lobby.bypass` で回避）、アイテムドロップ/ピックアップ制御

### 10.4 キュー（`QueueService` / `QueueCoordinator`）

- `QueueService`: キット×モード別のキュー。`QueueEntry(playerId, kitId, mode, elo, joinedAt, ip)`
  - `join()`（FFA 拒否・二重入場拒否）/ `leave()` / `pollMatches(blockSameIp, avoidRecent, now)`
  - `canMatch`: 同プレイヤー不可、同一 IP ブロック（設定フラグ）、直近対戦相手回避（設定フラグ）、アンランクは無条件、ランクは **初期 Elo 幅 75 + 15秒毎に 25 拡大**
  - `recentOpponents`: 直近対戦を記憶
- `QueueCoordinator`: 40 tick 毎にマッチング → `matchService.startDuel(...)`。20 tick 毎にアクションバー（待ち時間・待機人数）
  - `join()`: メンテナンス・キット無効・状態不正・二重入場を拒否 → Elo 取得（失敗時 1000 にフォールバック）→ キュー投入 → `QUEUED_RANKED/UNRANKED` 遷移 → 失明エフェクト 40tick → Leave Queue アイテム（RED_DYE, スロット4）付与 → サウンド・メッセージ
  - `leave()`: キュー離脱 → LOBBY リセット → ロビーインベントリ再適用

### 10.5 デュエル申請（`DuelRequestService` / `DuelCommand` / `DuelRequestGui`）

- `create(sender, target, kit, ranked, terrain, bestOf)`: TTL 60 秒、送信間隔 3 秒のレート制限、自己申請拒否
- 承認: `/accept` → `matchService.startDuel(...)`（ランク/アンランク、terrain、bestOf）
- 申請通知: チャット + `[CANCEL]` / `[ACCEPT]` / `[DENY]` クリック可能ボタン
- マッチ開始時に両者の保留申請を無効化（`invalidateForPlayer`）
- `DuelRequestGui`: 相手の頭（ping / 戦績 / K/D 表示）、キット選択・マップ選択・モード切替・Best of (1/3/5)・送信

### 10.6 マッチ進行（`MatchService` / `MatchRegistry` / `MatchListener` / `ArenaBoundsListener`）

`MatchState` 遷移: `CREATED → RESERVING_ARENA → PASTING_ARENA → WAITING_FOR_PLAYERS → COUNTDOWN → ACTIVE → ENDING → CLEANING → CLOSED`（失敗時 `FAILED`）

フロー:

1. `startDuel`: 二重登録チェック → Kit 有効チェック → `MatchSession` 作成 → `MatchRegistry.register`（プレイヤー・アリーナ二重防止）→ 申請無効化 → `PREPARING_MATCH` 遷移 → **MATCH FOUND タイトル + ジングル** → `arenaService.reserve(DUEL, terrain, matchId)`
2. アリーナ確保失敗 / 二重予約 → `failMatch`（「No arena available」等の理由付きメッセージ）
3. `WAITING_FOR_PLAYERS` → 0.5 秒後に両者を `teleportAsync`（両方成功するまで待つ）→ キット適用（個人レイアウト読み込み）→ `startCountdown`
4. カウントダウン: アクションバーに残り秒数 + サウンド → `beginFight`: `ACTIVE`、`FIGHTING` 遷移、スポーン再配置、FIGHT! タイトル、タイムアウトスケジュール
5. タイムアウト: kit.timeoutSeconds > 0 ならその値、無ければ max-duration-seconds（0=無効）。時間切れは引き分け
6. `handleLethal`（MatchListener がイベントをキャンセルして呼ぶ）: 自滅は引き分け、それ以外はキラー勝利 → `endMatch` → 結果プロセッサ → リマッチアイテム（LIME_DYE スロット3 / RED_DYE スロット5）→ エンド演出
7. `endMatch` 後: 勝敗タイトル / サウンド / Elo 変動メッセージ / 指定秒後にロビー帰還（`returnPlayersToLobby`）→ **auto-requeue 設定なら 10 tick 後に自動再キュー**
8. 再戦: 両者がリマッチ要求（`bothRematchRequested`）→ 別アリーナで `startDuel` 再実行

切断（`handleDisconnect`）:

- ACTIVE 前に切断 → `failMatch`（カウントダウン/準備中含む）
- ACTIVE 中に切断 → 相手勝利。`rumilance.punishment.bypass` を持たなければ **切断 ChatBan（既定 7 日）** を発行（shuttingDown 中は発行しない）
- `reconnect-grace-seconds` は現状フラグのみ（猶予実装は未接続）

`/leave`（`leaveDuringCountdown`）:

- COUNTDOWN 中のみ離脱可能。試合キャンセル（結果記録なし、双方 LOBBY へ）
- 連続カウントダウン離脱ストリークを記録（`countdownLeaveStreak`）。**3 回目で 3 日 ChatBan**。FIGHT 到達時にストリークリセット

`MatchListener` の戦闘ルール:

- COUNTDOWN / ENDING 中はダメージ無効、ACTIVE で参加者以外からのダメージは無効
- シールド破壊（`swordShieldBreak` + 剣攻撃 → 盾クールダウン 100tick）
- ノックバック倍率（次の tick で速度を倍率適用）
- 死亡イベントキャンセル（ドロップ無し・キープインベントリ）
- ブロック設置/破壊制御（`blockPlace` / `blockBreak` / `canBreak`）
- ドロップ禁止（COUNTDOWN / ENDING）、自動食料（`autoFood`）、自然回復禁止（`naturalHealthRegen=false` のとき SATIATED のみキャンセル）、エンダーパール制御（`pearl`）、トーテム判定（`totem`）
- リマッチ / ロビー帰還アイテムのクリック処理
- `ArenaBoundsListener`: アリーナ領域外への移動をスポーンに押し戻す（X/Z ブロック変化時のみ判定）

### 10.7 アリーナ（`ArenaService` 系）

- `AbstractArenaService`: テンプレート毎に 1 インスタンスを遅延生成、`ReentrantLock` で予約を直列化、`MatchRegistry` と二重にガード
- `SimpleArenaService`（FAWE 無し）: 状態切替のみ（ワールド触らず）
- `FaweArenaService`（FAWE 有り）: 解放時にテンプレートの `.schem` をアンカー（min コーナー）にペーストして再生成。schematic 未設定・FAWE 無効・`regenerate-arena=false` なら Simple と同挙動
- `FaweBridgeImpl`: WorldEdit API で `BlockArrayClipboard` 保存 / `ClipboardHolder` ペーストを `AsyncExecutor` 上で実行。初期化失敗（LinkageError 等）は `NoOpFaweBridge` へフォールバック
- `ArenaTemplateStore`: `arenas.yml` の読み書き。`/arena` コマンドで draft → save → enable の流れ。`persistAll` は `LocationUtil.serialize`（`world;x;y;z;yaw;pitch`）形式で spawn を保存

### 10.8 キット適用（`KitService`）

- 公式定義を `kits.yml` から読み込み（Map は小文字キー）
- `apply(player, kit, layout)`: レイアウト優先で装備（GRAY_STAINED_GLASS_PANE はスキップ）、HP・食料・ゲームモード（`forceAdventure` で ADVENTURE / それ以外 SURVIVAL）設定
- `/ekit` の個人レイアウトは `KitLayoutRepository` + `KitLayoutCache`（メモリキャッシュ、試合開始時に DB I/O しない）。`loadSyncIfAbsent` で同期ロード
- `/kit create` は現在の装備から定義を作成して保存

### 10.9 Elo 計算（`EloCalculator`）

- 期待値: `1 / (1 + 10^((相手Elo-自分Elo)/400))`
- Win=1.0 / Draw=0.5 / Loss=0.0
- K 値: 最初の 20 試合は 64、21 試合目以降 32、**上位 10% は 26**（暫定中でも優先）
- レート下限 0
- `RankedResultProcessor`: `tryMarkResultApplied` + 履歴重複チェックの冪等化 → 両者の `ranked_stats` を upsert → `match_history` 挿入 → `daily_ranked_stats` 加算（勝利者のみ kill+1、両者 match+1）
- 引き分けは `drawCountsAsLoss=true`（現状は敗北扱い）

### 10.10 観戦（`SpectatorService`）

- `/spec <player>` / `/specgui`: LOBBY / OPENING_GUI 状態のみ
- 対象マッチが ACTIVE で、両参加者の `spectateVisible` が true のときのみ
- SPECTATOR ゲームモード、他プレイヤーから hide（設定で）、退出時は show してロビーへ
- マッチ終了時は `clearMatch` で全観戦者をロビーへ

### 10.11 FFA（`FfaService` / `FfaListener`）

- `ffa.yml` のアリーナ（初期空）。`/ffa create` 等で管理
- `join()`: メンテナンス・無効・リセット中・状態・キット欠如チェック → `FFA` 遷移 → スポーン → キット適用
- `handleLethal`: 死亡処理（サウンド・セッション統計・非同期 DB 保存）→ キル者はアクションバー更新 → `respawn`
- `respawn`: 即時リスポーン（スポーンへ TP + キット再適用）
- ブロック変更を記録（`blockDiffs`、最大 50,000 件/アリーナ）→ `/ffa reset` で原状復帰（`BlockData` 復元）
- リージョン外移動はスポーンへ押し戻し、`resetting` 中のアリーナは参加不可
- キットルール（シールド破壊 / KB / ブロック / 食料 / 回復 / パール / トーテム）は MatchListener と同様の実装

### 10.12 設定（`SettingsService` / `PlayerSettings` / `/setting` GUI）

`/setting` で変更可能:

- デュエル申請拒否（`acceptDuelRequests`）
- 自動再キュー（`autoRequeue`）
- 観戦許可（`spectateVisible`）
- 他プレイヤーのチャット非表示（`hideOtherChat`）+ **チャットホワイトリスト**（名前をチャット入力で追加 / `clear` で全解除）
- デュエルサウンド（`soundsEnabled`）
- スコアボード（`scoreboardEnabled`）

キャッシュ + 非同期 upsert（`SettingsService.update/unload/flushAll`）。

### 10.13 スコアボード / TAB（`ScoreboardService`）

- 10〜20 tick 間隔の中央タスク（設定値は 10〜20 にクランプ）
- **v1.1.0 からミニマル構成**（太字なし・白/グレー基調）:
  - 常時表示: 鯖名（白）/ 鯖 IP（グレー）/ Online / Queue
  - 試合中: Kit / Mode / **スコア行（`0 - 0`）** / Time
  - キュー中: Queued: <kit>
  - ※ 旧来の Server / Matches / Lobby 行は削除
- **スコア行の仕様**: 左=自分（自分のチーム色で）右=相手（相手のチーム色で）を `自分n - 相手n` の形で表示。最初の試合は `0 - 0`。**再戦ボタンで連続対戦している間だけ**勝つたびに加算される（詳細は §10.21 / §19）
- 各プレイヤーの `scoreboardEnabled` と `scoreboard.enabled` で制御
- TAB ヘッダー/フッター: ヘッダー=鯖名（白）、フッター=鯖 IP + Online 数（グレー）

### 10.14 サウンド（`SoundService`）

- `sounds.yml` をキャッシュ。`play(player, id[, pitch])`
- プレイヤー設定 `soundsEnabled=false` なら再生しない
- `key: none` は無音扱い。`Registry.SOUNDS` で解決失敗時 `ui.button.click` フォールバック

### 10.15 多言語化（`LocaleService` / `MessageService`）

- ロケール正規化: 小文字化 + `-` → `_`
- フォールバック: 要求ロケール → デフォルト → `!key!`
- `MessageService`: MiniMessage デシリアライズ、`<tag>` プレースホルダ（`MessageService.tags("target", name, ...)`）
- チャットプレフィックスなし設計（`general.prefix` は空）

### 10.16 ChatBan / 異議申し立て（`ChatBanService`）

- `issue(target, staff, type, reason, duration)`: `punishments` + `audit_log` に非同期保存、メモリ `activeChatBan` に記録
- `issueDisconnectBan`: 試合中切断（既定 7 日）。`shuttingDown` 中は発行しない
- `isChatBanned`: DB 確認（失敗時メモリ確認）
- チャット（`AsyncChatEvent`）と私信コマンド（`/msg /tell /w /whisper /reply /r /message /pm`）を遮断
- `/objection <reason>`: ChatBan ID 単位で 1 回だけ異議を提出（`objections` テーブル、status=PENDING）
- `/chatunban <player|chatban_id>`: 解除

### 10.17 矢エフェクト（`ArrowEffectService`）

- 2 tick 毎の中央タスクで追跡中の矢を処理（`tracked` マップ）
- 射出時 `ProjectileLaunchEvent` で追跡開始。権限（mem / vip / vip_plus / OP）と設定 `arrowEffect` を確認
- 距離 48 ブロック以内の視聴者にのみパーティクル（プレイヤー毎 40 上限、設定可）
- 矢が死亡・無効・ブロック内に入ったら除去

### 10.18 オリジナルキット / プラン（`OriginalKitService` / `OriginalKitGui`）

- プラン: DEFAULT / MEMBER / VIP / VIP_PLUS（権限で判定）
- 解放スロット:
  - DEFAULT: 22 のみ
  - MEMBER: 13, 21, 22, 23, 31
  - VIP: 中央クロス（r0c4 / r1c3-5 / r2c2-6 / r3c3-5 / r4c4）
  - VIP_PLUS: 44 以外全て
- 月間編集回数制限（`plans.yml`: 10 / 30 / 100 / ∞）、月跨ぎでリセット
- 保存 / 読み込み / 共有コード（8 文字、メモリ保持、`ClientModBridge` は現状 NoOp）

### 10.19 統計・ランキング・プロフィール（`StatsService`）

- `kitStats` / `allKits` / `topElo(kit)` / `topEloOverall` / `topWinStreak` / `topDailyKills` / `topDailyMatches` / `recentRanked`
- `winRateLabel`: 21 試合未満は「計測中 n/21」
- `kd`: wins / max(1, losses)
- `profileMessage`: 区切り線 + MCID + 総試合/勝敗/勝率（計測中表示）/プログレスバー（profile.yml の char/length）/K/D/最高連勝/得意キット
- `nameOf`: オンライン優先、オフラインは OfflinePlayer 名、それも無ければ UUID 先頭 8 文字

### 10.20 実行時フラグ（`RuntimeFlags`）

- `maintenance`: `config.yml` の `plugin.maintenance` から初期化、`/practiceadmin maintenance on|off` で切替 + 永続化
- メンテ中はキュー・FFA・デュエル系を拒否（`rumilance.admin` は例外）

### 10.21 チームカラーと連戦スコア（v1.1.0）

**チームカラー（重複禁止）**

- デュエルでは参加者 index 0 が常に **RED**、index 1 が常に **BLUE**（`MatchSession` の `teamColors`、enum `TeamColor`）
- 2 名しかいないため色の重複は構造上発生しない（重複禁止）
- 再戦チェーンでは参加者順序が同じままなので、**連戦中は自分の色が変わらない**
- カウントダウン開始時に「あなたは RED/BLUE チームです。」を通知（`match.team-red` / `match.team-blue` キー）

**連戦スコア（0-0 → 1-2 …）**

- `MatchSession` が `seriesWins`（参加者ごとの連戦勝利数）を保持
- 最初の試合（キュー・デュエル申請由来）は `seriesWins` 空 → スコアボードは `0 - 0`
- 試合終了時に勝者へ `addSeriesWin`（`endMatch`）
- **再戦ボタン**で双方がリマッチを承認すると、前セッションの `seriesWinsSnapshot()` を新セッションへ引き継いで `startDuel(...)`（`applySeries`）
- したがって「同じ相手でも、再戦を押して連続で戦った場合のみ」スコアが加算され、キューや新規申請で組み直した場合は 0-0 に戻る
- スコアボード表示: 自分から見て左=自分の勝利数（自分の色）、右=相手の勝利数（相手の色）。例: RED の自分なら `§c1 §7- §90`、BLUE の自分なら `§91 §7- §c0`

---

## 11. GUI 一覧

`GuiType` 全 16 種。識別は `PracticeGuiHolder`（セッションUUID + タイプ）:

| GuiType | クラス | サイズ | 内容 |
|---|---|---|---|
| RANKED_QUEUE | `QueueKitGui`(ranked) | 54 | キット選択 → ランクキュー参加。待機人数表示、無効キットは BARRIER |
| UNRANKED_QUEUE | `QueueKitGui`(unranked) | 54 | 同上（アンランク） |
| DUEL_REQUEST | `DuelRequestGui` | 54 | 相手ヘッド（ping/戦績/K/D）、キット・マップ・モード・BestOf・送信 |
| KIT_SELECT | `KitSelectGui` | 54 | キット選択（戻る/選択、一時選択 temp_kit） |
| MAP_SELECT | `MapSelectGui` | 27 | CRYSTAL / FLAT / ANY / BUMPY / NETHERITE（一時選択 temp_map） |
| PLAYERS | `PlayersGui` | 54 | オンライン一覧（28/ページ、ページ送り、状態・Ping・勝率表示）→ デュエル申請 |
| SETTINGS | `SettingsGui` | 36 | 6 トグル + チャットホワイトリスト |
| SPECTATE_LIST | `SpectateListGui` | 54 | 進行中マッチ一覧（A vs B・Kit・Mode）→ 観戦 |
| STATS_KIT | `StatsKitGui` | 54 | キット別ランク統計（Wins/Losses/Matches/WinRate/K/D/Streak/Elo） |
| EDIT_KIT | `EditKitGui` | 45 | `/ekit`。ピッカー → レイアウト編集（防具列・メイン・ホットバー、クリック交換、**並べ替えのみ許可**（内容同一性検証）、Save/Back） |
| ORIGINAL_KIT | `OriginalKitGui` | 45 | プラン解放スロット表示、Save/Load/Share/Close、月間編集回数アクションバー |
| FFA_LIST | `FfaListGui` | 36 | FFA アリーナ一覧 → 参加 |
| ARROW_EFFECT | `ArrowEffectGui` | 27 | 矢エフェクト選択 |
| ADMIN_MENU | （未使用の enum） | – | 予約値 |
| KIT_ADMIN | `KitAdminGui` | 54 | `/kit`。キット一覧 → 設定トグル（**日本語固定** FORCED_LOCALE=ja_jp） |
| PROFILE | `ProfileGui` | 54 | `/profile [player]`。プレイヤー頭 + サマリー（総試合/勝利/敗北/勝率/K-D/最高連勝/得意キット/Best Elo）+ キット別統計（Elo/W-L/勝率/K-D/連勝/Best） |

GUI 基盤:

- `AbstractGui.open()`: `GuiSessionRegistry.open` → `PracticeGuiHolder` → `Bukkit.createInventory` → `render` → `player.openInventory` → gui-open サウンド
- `GuiListener`: PracticeGuiHolder のみ処理。全クリックをキャンセルし、数値キー・ドラッグ・シフトクリック・ホットバー交換等は無視、装飾アイテム（action=decorate）は無視、古いセッションはクローズ
- `GuiSession`: 選択状態（selectedKit / selectedMap / page / bestOf / ranked / targetPlayer / attributes）
- `GuiDecorator`: `gui_action` PDC タグ付きボタン生成（borderless デザイン）

---

## 12. アイテムタグ（PDC キー）一覧

`ItemKeys`（名前空間 `rumilancepractice:<key>`）:

| キー | 用途 |
|---|---|
| `kit_name` | キット名タグ |
| `arena_id` | アリーナ ID タグ |
| `gui_action` | GUI ボタンアクション（例: `kit:nodebuff` / `close` / `toggle:...` / `player:<uuid>` / `spec:<uuid>` / `ffa:<id>` / `fx:<id>` / `slot:<n>` / `editkit:<name>` 等） |
| `arrow_effect` | 矢エフェクト |
| `original_kit_marker` | オリジナルキットマーカー |
| `function_type` | ロビー機能アイテム（ranked / unranked / ffa / ekit / settings / spectate / leavequeue） |
| `admin_tool` | 管理者ツール（region / menu） |
| `leave_queue` | キュー離脱アイテム（RED_DYE） |
| `rematch` | リマッチアイテム（LIME_DYE） |
| `return_lobby` | ロビー帰還アイテム（RED_DYE） |
| `target_uuid` | 対象プレイヤー UUID |

---

## 13. リスナー一覧

| リスナー | 登録イベント |
|---|---|
| `SessionBootstrapListener` | PlayerJoin（LOWEST: セッション作成・設定反映・LOBBY 初期化・ロビー送り・レイアウト preload・PlayerData upsert）、PlayerQuit（MONITOR: 設定 flush・last_seen 更新・セッション除去） |
| `LobbyListener` | EntityDamage / FoodLevelChange / BlockBreak / BlockPlace / PlayerDropItem / EntityPickupItem / PlayerMove（落下時ロビー帰還） |
| `MatchListener` | EntityDamage(HIGHEST) / PlayerDeath / BlockPlace / BlockBreak / PlayerDropItem / FoodLevelChange / EntityRegainHealth / PlayerInteract（パール・リマッチ・ロビー）/ PlayerQuit |
| `ArenaBoundsListener` | PlayerMove（領域外押し戻し） |
| `FfaListener` | EntityDamage(HIGHEST) / PlayerDeath / PlayerRespawn / PlayerMove / BlockPlace / BlockBreak / FoodLevelChange / EntityRegainHealth / PlayerInteract / PlayerQuit |
| `GuiListener` | InventoryClick(HIGH) / InventoryDrag / InventoryClose |
| `FunctionalItemListener` | PlayerInteract（機能アイテム） |
| `AdminToolListener` | PlayerInteract（リージョン選択・セットアップメニュー） |
| `PracticeSideListener` | AsyncChatEvent（ChatBan 遮断・ホワイトリスト入力・他チャット非表示）、PlayerCommandPreprocessEvent（私信遮断）、ProjectileLaunchEvent（矢追跡）、PlayerQuit（設定 flush・観戦/FFA 離脱） |

---

## 14. データモデル一覧

| モデル | フィールド |
|---|---|
| `ArenaInstance` | id, template, state(AVAILABLE/IN_USE/REGENERATING/DISABLED), currentMatchId |
| `ArenaTemplate` | id, name, type, terrain, world, min/max(XYZ), serializedSpawnA/B, schematicPath, enabled |
| `AuditLogEntry` | id, actorUuid, action, details, createdAt |
| `DuelRequest` | id, sender, target, kitName, ranked, createdAt, expiresAt |
| `KitDefinition` | name, displayName, icon, ranked, ffaEnabled, maxHealth, naturalHealthRegen, knockbackMultiplier, items, armor, enabled, arenaTerrain, autoFood, swordShieldBreak, blockPlace, blockBreak, canBreak, pearl, totem, forceAdventure, timeoutSeconds |
| `KitItemEntry` | slot, material, amount, displayName, itemDataBase64 |
| `KitLayoutSnapshot` | id, uuid, kit, itemDataBase64, updatedAt |
| `MatchHistoryEntry` | id, playerA, playerB, kit, mode, winner, ranked, startedAt, endedAt |
| `OriginalKitSnapshot` | uuid, itemDataBase64, armorDataBase64, savedAt |
| `PlayerData` | uuid, username, firstJoin, lastSeen, locale |
| `PlayerSettings` | uuid, soundsEnabled, scoreboardEnabled, arrowEffect, spectateVisible, acceptDuelRequests, autoRequeue, hideOtherChat, chatWhitelist, locale |
| `PunishmentRecord` | id, targetUuid, staffUuid, type, reason, issuedAt, expiresAt, revoked |
| `RankedKitStats` | id, uuid, kit, elo, wins, losses, winStreak, bestElo |
| `UnrankedStats` | wins, losses, winStreak, lossStreak |

---

## 15. ユーティリティ一覧

| クラス | 内容 |
|---|---|
| `AsyncExecutor` | 1〜4 スレッドのプール（`RumilancePractice-Worker-N`、daemon）。supplyAsync / runAsync / execute / shutdown |
| `Cuboid` | イミュータブルな軸平行領域（正規化、contains / volume / center / expand） |
| `GuiSlots` | スロット計算（slot / row / column / range / border、純粋関数でテスト可） |
| `ItemKeys` | PDC キー一覧（`init` 必須） |
| `ItemSerializer` | `serializeAsBytes()` ベースの ItemStack 配列シリアライズ（Base64 対応、旧 Java 直列化不使用） |
| `LocationUtil` | 座標シリアライズ（`world;x;y;z;yaw;pitch`）、ブロック中央、WorldBorder セーフテレポート（`clampXZ`、マージン 2.0、内側は 1.5 に縮小） |

---

## 16. テスト一覧

| テスト | 検証内容 |
|---|---|
| `ArenaSlotAllocatorTest` | 同一インスタンス二重予約不可 / 複数テンプレート時は別インスタンス割当 |
| `DuelRequestServiceTest` | 期限切れ申請は承認不可 / invalidate で送受信とも消える |
| `EloCalculatorTest` | 最初20試合 K=64 / 21試合目以降 K=32 / 上位10% K=26（暫定中でも）/ 期待値・下限0等 |
| `LocaleServiceTest` | 同梱ロケール / 要求ロケール解決 / デフォルトへのフォールバック |
| `MatchRegistryTest` | プレイヤー二重登録拒否 / アリーナ二重予約拒否 |
| `UnrankedStatsImmutabilityTest` | イミュータブル性 |
| `MatchSessionIdempotencyTest` | 結果適用 1 回のみ / 切断ペナルティ 1 回のみ + シャットダウン時スキップ |
| `PlayerStateManagerTest` | 不明プレイヤーは IDLE / initialize は LOBBY / 正当遷移 / 不正遷移は例外 / resetToLobby |
| `GuiSlotsTest` | スロット計算・範囲チェック |
| `LocationUtilWorldBorderTest` | ボーダー外クランプ / 端付近の内側引き込み / 余裕のある位置はそのまま |

---

## 17. 管理者セットアップ手順

1. `/practiceadmin tool` → Region Selector（ブレイズロッド）と Practice Setup Menu（ネザースター）を取得
2. `/slobby pos1` → `/slobby pos2` でロビー領域を設定
3. `/slobby spawn` でロビースポーンを設定（ボーダー外なら警告が出る）
4. `/setlobbyitem` でロビーインベントリを保存
5. `/setfunc ranked` などで機能アイテムを作り、ロビーインベントリへ配置（手持ちに付与 → `/setlobbyitem` で再保存）
6. `arenas.yml` に DUEL テンプレート（spawn-a / spawn-b）を追加、または `/arena draft` → `selection apply` → `p1/p2` → `save` → `enable` で登録（FAWE 推奨）
7. `kits.yml` を確認（nodebuff / boxing / sumo / gapple 同梱）。`/kit` で管理 GUI
8. `/slobby validate` と `/practiceadmin status` で最終確認
9. 必要なら `/kit create <name>`（現在の装備から）/ `/ekit` で個人レイアウト / `/setfunc` で機能アイテム拡充

---

## 18. トラブルシューティング・注意点

| 症状 | 原因・対処 |
|---|---|
| 「FastAsyncWorldEdit/WorldEdit not detected」警告 | FAWE/WE 未導入。導入するか `config.yml` の `fawe.enabled: false` |
| 試合が始まらない | `arenas.yml` に enabled な DUEL テンプレートがあるか確認。ワールド名・座標・スポーンが正しいか |
| キューに入れない | プレイヤーが LOBBY/OPENING_GUI 状態か、キットが enabled か、メンテナンス中でないか |
| DB エラー | `database.yml`（標準 SQLite）とデータフォルダの書込み権限。SQLite は `data/` ディレクトリ自動作成 |
| 起動時にプラグイン無効 | `initializeDatabase` 失敗時は「Failed to initialize the database. Disabling RumilancePractice.」+ スタックトレース。thin jar を使用していないか（`-thin` 付きは DB ドライバなしで失敗する） |
| コマンドが「なんかエラーが発生しました」系 | サーバーコンソール / `logs/latest.log` のスタックトレース（`Caused by:`）が必要。ログを確認して `RumilancePractice` 行と `Exception` 行を探す |
| 表示が `!key!` になる | 翻訳キー不足。`lang/` のロケールファイル確認 |
| 効果音が鳴らない | `sounds.yml` の key が `none` / プレイヤー設定の Duel Sounds OFF / キー名が 1.21 に無い |
| 設定変更が反映されない | `/practiceadmin reload`（kits / arenas / ffa / lobby / sounds）。DB やプラグイン全体は再起動 |
| 切断でいきなり ChatBan | 試合 ACTIVE 中の切断は既定 7 日 ChatBan（`rumilance.punishment.bypass` で回避可）。サーバー停止時は発行されない |

### 現状の未実装・注意点（コード調査によるメモ）

- `general.unknown-error`（「予期しないエラーが発生しました。管理者に連絡してください。」）は言語ファイルに定義済みだが、**Java コードからは参照されていない**（Bukkit 側の汎用エラーメッセージが出ている場合はサーバーログのスタックトレースが手掛かり）
- `gui.yml` / `profile.yml` の一部項目 / `hooks.placeholderapi` / `ffa.yml` の settings 群 / `reconnect-grace-seconds` は現状コード上で使用されていない（将来用）
- `ClientModBridge` は常に NoOp
- スコアボードの「Lobby」行は描画のみ（表示名タグの衝突回避は `§` コードで行っている）
- `QueueKitGui` / `StatsKitGui` などの GUI タイトルはローカライズされていないものがある（一部日本語・英語混在）

---

## 19. UI デザイン方針（v1.1.0）

開発者向けの指針。今後の UI 変更は以下に従うこと。

### 19.1 基本ポリシー（ミニマリスト）

| ルール | 内容 |
|---|---|
| 太字禁止 | 見出し・タイトル・アイテム名に `TextDecoration.BOLD` を使わない（勝敗タイトル等の演出系のみ例外的に許可） |
| 色は最小限 | ラベル=グレー、値=白 を基本。赤/青・緑/赤（ON/OFF 等）は「機能色」としてのみ使用 |
| 情報は詳細に、構造は単純に | 数は多くてもいいが、1 画面の構造（行・列・意味）を単純に保つ |
| 装飾アイテム | `GuiDecorator.decorative`（グレー染色ガラス等）は敷き詰めすぎない |

### 19.2 スコアボード仕様（§10.13 参照）

```
§f<Rumilance>          ← scoreboard.server-name（白）
§7<play.example.com>   ← scoreboard.server-ip（グレー）
§7Online: §f<n>
§7Queue: §f<n>

（試合中のみ）
§fKit: §7<kit>
§fMode: §7<mode>
§c1 §7- §90            ← 連戦スコア（自分の色 / 相手の色）
§7Time: §f<sec>s
```

- サイドバータイトル・TAB ヘッダーも鯖名（白）
- スコアの加算条件は「再戦ボタンによる連続対戦のみ」（§10.21）

### 19.3 プロフィール GUI（`ProfileGui`）

- ヘッダー: プレイヤーヘッド（名前・Ping/Offline）
- サマリー 2 行: 総試合数 / 勝利 / 敗北 / 勝率（計測中 n/21 表示）、K/D / 最高連勝 / 得意キット / Best Elo
- キット別: アイコン + Elo / W-L / 勝率 / K/D / 連勝 / Best
- 配色: ラベル グレー・値 白のみ（アイコン素材の色はそのまま）

### 19.4 設定 GUI（`SettingsGui`）

- タイトル白・太字なし。トグル名はグレー、ON=緑 / OFF=赤 の機能色のみ
