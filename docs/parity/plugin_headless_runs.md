# プラグイン側BOT ヘッドレス実測ログ(石100床・通常戦)

参照 = 実 QuantumBOT(Fabric + Quantum's PvP Practice v1.18, difficulty 2)。
プラグイン側 = NARENA の練習BOT を `narena-harness`(開発フラグ `-Drumilance.harness=true`)で
**普通に戦わせた**だけ。シナリオ固定・対象凍結・特別ラウンドは一切なし。

## 計測環境(両者で同じ条件にそろえたもの)

| 項目 | 参照(QuantumBot) | プラグイン(NARENA) |
|---|---|---|
| 床 | 石 94 ブロック(y=-37..56 付近) | 石 **100** ブロック(`ground 40 100` → 81×81, y=-37..63) |
| 相手 | 対戦相手プレイヤー(自動で戦う) | 偽プレイヤー `HarnessBot`(通常の join→countdown→match) |
| ラウンド | マップ標準(通常戦・自動開始) | 通常戦(難易度 INTERMEDIATE = map difficulty 2) |
| 計測 | qlog 1行/毎tick(`[q] ... anc= ct= pc= hit=`)+ 距離バンド d2/d3/d6/d9 | `[N Arena][BotMatch]` trace(0.1s)+ samples(0.1s) |
| 長さ | 420 秒 | 145〜150 秒 × 複数本 |

距離バンドは参照側 qlog に追加した計測列(相手との距離がそれぞれ 2/3/6/9 ブロック以内か)。
プラグイン側は samples の座標と相手の座標から `tools/fight_profile.py` が同じ形に集計する。

## 参照(実 QuantumBOT)の実測値 — 420 秒 / difficulty 2

```
距離滞在: <=2:4.6%  <=3:3.5%  <=6:37.3%  <=9:2.5%  >9:52.1%
行動                    回数    /min   距離バンド内訳
pearl throw          345    49.2   中央ギャップ  1.00s | <=2:  1%  <=3:  1%  <=6: 29%  <=9:  3%  >9: 66%
anchor place         127    18.1   中央ギャップ  0.60s | <=2: 21%  <=3: 10%  <=6: 69%
anchor charge        128    18.3   中央ギャップ  0.60s | <=2: 27%  <=3:  5%  <=6: 68%
crystal place         37     5.3   中央ギャップ  8.75s | <=2: 49%  <=3: 19%  <=6: 27%  <=9:  5%
melee swing           80    11.4   中央ギャップ  0.50s | <=2: 46%  <=3: 54%
手持ち: ender_pearl 76.5% / totem 7.2% / glowstone 5.7% / respawn_anchor 5.5% / end_crystal 2.7% / diamond_sword 2.3%
```

読み取れること(これがプラグイン側の設計根拠になった):

1. **アンカーは近距離でも撃つ**。127 回のうち 31% が 3 ブロック以内、69% が 3〜6 ブロック。
   → 「近接中はアンカー封印」は誤り。map `bin/27` の `cannot_anchor`(殴った直後の hitcd 6..1 は
   `g1gc/anchor_tick` が return)が本当のゲート。
2. **パールは距離を見ない**。1 秒毎(中央ギャップ 1.00s)に投げ、2 ブロック以内でも 1% は投げる。
   1 ブロック以内だけは 0(着地点が相手のヒットボックス内になるため)。
3. **剣は 3 ブロック以内のみ**、中央ギャップ 0.50s(= hitcd 相当)。
4. 立ち回り: 半分以上の時間を 9 ブロック以遠で過ごし、パールで間合いを詰める。
   → 近接に張り付いたままの相手(こちらの偽プレイヤー)では件数の絶対値が変わるため、
   比較は「距離バンド内訳」で行う必要がある。

## プラグイン側の推移(石100床・通常戦)

| run | version | 試合長 | アンカー place/charge/detonate | クリスタル place | パール | 剣 swing/hit |
|---|---|---|---|---|---|---|
| 1 | 1.76.10 | 145s | 1 / 1 / 1(7.0→7.2→7.6s = 4t/8t)| 16 | 0 | 107 / 106 |
| 3 | 1.76.13 | 145s | 1 / 1 / 1 | 16 | 1 | 150 / 150(※trace 300件上限で打切) |
| 4 | 1.76.14 | 145s | 0 | 16 | 1 | 413 / 413 |
| 5 | 1.76.15 | 145s | 16 / 16 / 16(0.70s 周期) | 20 | 1 | 401 / 401 |
| 6 | 1.76.16 | 145s | 200 / 200 / 200(過剰)| 24 | 1 | 327 / 327 |
| 7 | 1.76.18(ローカル overlay) | 145s | 72 / 72 / 72 | 33 | 73 out + 72 in | 181 / 181 |

run5 のアンカー列(抜粋。1.76.15 のログ):

```
 7.3s anchor place   8.0s anchor place   8.7s anchor place   9.4s anchor place
 7.5s anchor charge  8.2s anchor charge  8.9s anchor charge  9.6s anchor charge
 7.7s anchor detonate 8.4s anchor detonate 9.1s anchor detonate 9.8s anchor detonate
```

place→charge→detonate が map の rung(`anchor_cd` 4t / `charge_cd` 4t / `explosion_cd` 4t)通りに
回っている。ただし 33 秒で停止した = **弾切れ**(アンカー16個 + グロウストーン64個 = チャージ16回分)。
v1.76.16 でパールと同じ補充を入れた。

## 直したこと(実測 → 原因 → 修正)

| 症状 | 原因 | 修正 |
|---|---|---|
| 近接中はアンカーが 1 回で止まる | `dist <= 3.0` で engage を封印していた | map `bin/27` の `cannot_anchor` を自分のスイング間隔で再現(v1.76.15) |
| 相手の hurt frame 待ちにしたが窓が開かない | 偽プレイヤーはプラグイン独自のダメージ登録で i-frame が付かない | 同上(自分の hitcd を窓の基準に) |
| パールが 1 回で止まる | `findPearlLanding` が 2.0 ブロック未満の候補を全部捨てていた | 近接ホップも許可(v1.76.16) |
| パールの着地が相手の体内 | 近接時に「0.9 ブロック以内の着地」を弾いていた | map の ray は相手ヒットボックスで止まるので、そのまま採用(v1.76.15) |
| アンカーが 33 秒で停止 | アンカー16 + グロウストーン64(4個/チャージ)の弾切れ | パールと同じ補充(v1.76.16) |
| 剣ログが 60 秒で途切れる | chatter(swing/hit)バジェット 300 件 | 3000 件に拡大(v1.76.14) |

## 使い方

```
# 参照側(距離バンド付き)
python3 tools/fight_profile.py ref /tmp/ref_dist_run.log

# プラグイン側(相手の座標を渡す)
python3 tools/fight_profile.py plugin /tmp/paper-run/logs/latest.log --dummy 0.5,64,0.5
```

## run7(1.76.18)の実測 — 参照と同じ土俵での比較

```
行動               何/分      参照(全体)   参照(距離バンド内)
pearl              60.0       49.2         31(≤6ブロック滞在 191 秒あたり)
anchor place       29.8       18.1         40(同上)
crystal place      13.7        5.3         11(同上)
melee swing        74.9       11.4        141(≤3ブロック滞在 34 秒あたり)
手持ち: sword 42.7% / pearl 27.0% / glowstone 11.7% / end_crystal 8.4% / bow 6.4% / anchor 3.8%
```

- パール: 受動(離脱)+ チェイスで 60/min。参照 49.2/min の 1.2 倍まで来た(合計は `pearlcd` 20t 共有)。
- アンカー: 連鎖の再開間隔を実測値(約1.4秒)に合わせて 82.8 → 29.8/min。参照の 6 ブロック以内
  レート(40/min)と全体値(18/min)の間に収まった。
- クリスタル: 13.7/min は参照の「バンド内 11/min」とほぼ同水準(全体 5.3/min に対しては 2.6 倍)。
- 剣: 74.9/min は参照の全体値(11.4)より多いが、これは**相手が押し返さない**ためで、
  参照は 91% の時間を 3 ブロック以遠で過ごす(こちらは 99.9% が 2 ブロック以内)。
  参照の「3 ブロック以内滞在あたり」は 141/min なので、剣の間隔そのものは同等(7 tick = 350 ms)。


## 参照(実 QuantumBOT)の「距離バンド別」実測 — 420 秒・difficulty 2

`/tmp/ref_dist_run.log`(8409 tick, 420.45 s, d2/d3/d6/d9 付き qlog)から。

| 距離 | 滞在 | 割合 | anchor place | pearl | swing |
|---|---|---|---|---|---|
| ≤2 | 19.4 s | 4.6% | 47 (145/min of dwell) | 3 | 37 (114/min) |
| ≤3 | 14.7 s | 3.5% | 31 (131/min) | 5 | 43 (176/min) |
| ≤6 | 156.9 s | 37.3% | 39 (14.9/min) | 100 | 0 |
| ≤9 | 10.5 s | 2.5% | 2 (17.1/min) | 10 | 0 |
| >9 | 218.9 s | 52.1% | 2 | 227 (62.2/min) | 0 |
| **合計** | 420.5 s | | **121 (17.3/min)** | **345 (49.2/min)** | **80 (11.4/min)** |

読み取れること:

- **アンカーは「近距離にいる間だけ、ラダー全速(約0.4秒/連鎖)」**。2 ブロック以内の滞在 19.4 秒で
  47 連鎖 = 145/min。参照は 6 ブロック以内に 45% しか居ないので、全体では 17.3/min になる。
  「1 秒に 1 回」でも「1.7 秒に 1 回」でもなく、**滞在時間が先、頻度は従属**。
- **剣は 3 ブロック以内のみ**、しかも 0.34 秒間隔(7 tick)の全速。全体 11.4/min は滞在が 8% しか
  ないため。バンド外では 1 回も振っていない。
- **パールは >9 ブロック滞在の 62/min**(=ほぼ pearlcd 20 tick の上限)。近距離ではほとんど投げない
  (≤2 は 1%)。
- つまり参照の rhythm は「遠くでパールを投げ続けて接近 → 近づいた瞬間だけアンカー/剣を全速 →
  すぐ離れる」。プラグイン側が 49% を 2 ブロック以内で過ごすのが、頻度差の根本原因。

## アイテム保持の実測(ref 405 s / plugin run8)

| アイテム | ref | plugin 1.76.18 | plugin 1.76.19 |
|---|---|---|---|
| ENDER_PEARL | 76.5% | 27.0% | **61.4%** |
| NETHERITE_SWORD | 2.3% | 42.7% | 15.0% |
| TOTEM_OF_UNDYING | 7.2% | 0.0% | **7.0%** |
| GLOWSTONE | 5.7% | 11.7% | 6.8% |
| END_CRYSTAL | 2.7% | 8.4% | 5.7% |
| RESPAWN_ANCHOR | 5.5% | 3.8% | 4.0% |
| BOW | 0.0% | 6.4% | 0.1% |

ref の遷移(405 秒): `respawn_anchor->glowstone` 104、`glowstone->totem_of_undying` 96、
`totem->respawn_anchor` 69、`totem->pearl` 30、`end_crystal->pearl` 29、
`sword->respawn_anchor` 28。1.76.19 の遷移上位は `sword->pearl` 144 / `pearl->sword` 134 /
`glowstone->pearl` 33 / `pearl->respawn_anchor` 31 / `glowstone->totem` 27 で、文法は一致。

## 残差

- 剣 15.0%(ref 2.3%)と melee 77/min(ref 11.4)は**滞在分布**の従属変数。参照は 3 ブロック以内に
  8% しか居ないので、まず接近→離脱の rhythm(バースト)を合わせる必要がある。
- パール 59.6/min(ref 49.2)。参照は近距離では投げない(≤2 で 1%)ため、受動パールの距離ゲート
  (≤8 ブロック)を「ピンチ時のみ」に寄せる余地がある。
- アンカーは 1.76.19 で 29.8 -> 20.3/min(ref 17.3、目標帯 16.5-18)。

## 残っている差(2026-09-15 時点)

- パール: run5 まで 0.4/min。v1.76.16 で 1 秒毎に戻る見込み(参照 49.2/min)。
- アンカー: run5 の 6.6/min(停止込み)。v1.76.16 以降は周期 0.7 秒 × 途切れなしが期待値で、
  参照の「距離バンド内」レート(≤6ブロック滞在 191 秒あたり 100 回 = 31/min)と比較する。
- 剣: こちらの偽相手は押し返さないので距離 ≤2 に張り付き、165/min(参照は 11.4/min だが
  参照BOTは 91% の時間を 3 ブロック以遠で過ごす。バンド内では 141/min)。

## 2026-09-15: 距離バンドの再計測と「誰がズレているか」の切り分け

同じ土俵(石100床・通常戦・難易度 INTERMEDIATE・145秒)で、map の実装を読み直したうえで
BOT 側を map 準拠に寄せた。参照(実 QuantumBOT 420秒)と現在のプラグイン側を並べる。

| 指標 | 参照(実機420s) | b18 | b19 | b20 | b22(現在) |
|---|---|---|---|---|---|
| 滞在 <=2 | 4.6% | 3.7 | 2.3 | 5.3 | 4.6 |
| 滞在 <=3 | 3.5% | 5.2 | 2.2 | 5.6 | 4.3 |
| 滞在 <=6 | **37.3%** | 33.2 | 16.8 | 24.2 | 21.9 |
| 滞在 <=9 | 2.5% | 7.7 | 8.3 | 7.8 | 3.6 |
| 滞在 >9 | 52.1% | 50.1 | 70.3 | 57.1 | 65.6 |
| pearl /min | 49.2 | 57.1 | 41.0 | 50.1 | 38.9 |
| anchor place /min | 18.1 | 31.4 | 15.7 | 22.8 | 23.2 |
| anchor charge /min | 18.3 | 30.6 | 15.7 | 22.8 | 21.5 |
| crystal place /min | 5.3 | 5.0 | 0.4 | 11.2 | 6.6 |
| melee swing /min | 11.4 | 27.7 | 10.3 | 18.2 | 19.0 |
| gap eat(hp16) | あり | 2回 | 1回 | 2回 | 1回 |

設定の違い(b18/b20/b22 は同じ map 準拠ロジックで、下の2つだけが違う):

| 版 | アンカー連鎖 | 立ち位置 | 逃げパール後の passive 窓 |
|---|---|---|---|
| b18 | 12t (0.6s) | 3.0〜4.5 | 0(なし) |
| b19 | 12t | 3.5〜4.5 | 0 |
| b20 | 20t (1.0s) | 3.0〜4.5 | 0 |
| b22 | 12t | 3.0〜4.5 | 2000ms |

### 今回 map から拾って直したもの(すべて `quantum/` の実装が根拠)

1. **アンカーの可視範囲**: `mark/mark_main` は `loc1`(=アンカーの実ブロック位置)マーカーにしか
   `usable` を付けず、その条件が「ボットの目から **4.6 ブロック以内**」。8 ブロックは
   *マークを立てる*半径にすぎない。→ `anchorReach = 4.6`。
   6〜9 ブロックからアンカーを撃っていたのが、参照に無い「6〜9 滞在 24%」の主因だった。
2. **クリスタルの可視範囲は 3.5 ブロック**(同じ `mark_main`)。以前は 9 ブロックまで置いており、
   クリスタル 21.9/min(参照 5.3)になっていた。→ 3.5 で参照とほぼ一致(5.0〜6.6/min)。
3. **追いパールは相手のブロックで止まる**: `ray/cast2 → ray/pearlstep` は
   「`@a[tag=xlib_target,dx=0]` になったら return」= 着地点は相手の足元。以前は 15 ブロック上限で
   6 ブロック手前に着地し、そこから 6〜9 を *歩いて* いた(滞在 24%)。→ 上限を外して一気に着地。
4. **逃げパールは 4.6〜10 ブロック**(`crystal/passive/main` の 8..10 / 8..15 分岐)。
   近距離(=マーカーが立っている=戦闘中)からは投げない、が正しい読み。
5. **立ち位置 3〜4.5 ブロック**(`g1gc/movement` は crystal マーカーが 2.5〜3.5 の位置に来るよう
   動き、`move forward` はアンカーの各タイマー中は拒否される)。

### 残差は「BOT が濃すぎる」= 相手(ハーネス)側の問題でもある

参照の 420 秒を1tick単位で数え直すと、**アンカー/クリスタルの機構が動いているのは全 tick の
20.4% だけ**で、連鎖の中央値は 11 tick・間の空きは中央値 19 tick/平均 69 tick。つまり実機の BOT は
「12 tick の連鎖 → 1 秒前後の間 → また連鎖」を繰り返している。こちらは 1 連鎖あたり 1 発の剣＋
1 連鎖までは同じだが、**再エンゲージまでが 1 秒(逃げ→追いパール)しかなく、連鎖が途切れない**。
b22 で逃げ後に 2 秒の passive(徒歩で戻る・追いパール禁止)を入れて周期を 3 秒まで伸ばしたが、
参照の 5.25 秒(剣 80 発 / 420 秒)にはまだ足りない。

実機の 5 秒周期は「相手(プレイヤー)が動く・殴る・跳ぶ」ことでマーカーが頻繁に無効化される為で、
**棒立ちで殴ってこない偽プレイヤー相手では原理的に再現しない**。この差は BOT のロジックではなく
相手モデル側の差なので、次のどちらかで詰める:

- ハーネスの相手を「普通に動く相手」(前進/後退/ジャンプ/ノックバック)にする ← 本筋
- もしくは BOT 側の再エンゲージ周期を `PASSIVE_ESCAPE_WINDOW_MS`(現在 2000ms)で調整する

`PASSIVE_ESCAPE_WINDOW_MS` を 0 にすると b18 の値(滞在 <=6 33.2% / アンカー 31.4/min)、
2000 で b22(21.9% / 23.2)になる。参照は 37.3% / 18.1 なので、相手モデルを直したうえで
この値を詰めるのが正しい順序。

## 2026-09-15 (2): 「ハーネスのせいか、BOTのせいか」の切り分けと、参照の本当の周期

### 結論: 相手(ハーネス)は原因ではない — 原因は BOT 側の「状態機械」が丸ごと無いこと

参照側の相手が何だったのかを `docs/parity/README.md` と `tools/qlog-datapack` で確認した:

- 参照の相手は `/playerspawn target1` で出した**ただのフェイクプレイヤー**(`quantum:miscellaneous/tags` が
  `xlib_target` を付けるだけで、ピン留めも AI も無い)。移動も攻撃もしない、こちらの `HarnessBot` と同型。
- ただし `qlog:keepalive` が毎tick
  `effect give @a[tag=xlib_target] resistance 2 1 true` + `regeneration 2 3 true` を与えて**延命している**
  (README: 「相手HP 20.0 → 最小 2.0、実ダメージ 18」= 抵抗と再生込みで 20→2 まで削られている)。
- 参照のBOT自身は**自分のアンカー爆破で自分を削っている**。run8 の 34 秒アンカー窓(下記)で
  HP 20.0 → 3.7 まで落ち、403 秒全体で hp 平均 16.8 / 最小 0.0 / トーテム POP 3 回。

つまり「こちらの偽プレイヤーが動かない/殴らないから件数が増える」は**言い訳にならない**。
参照も同じ棒立ち相手で、しかも 145 秒で測ったこちらの方が**行動回数が 2〜3 倍**だった。
差分は BOT の内部にあり、正体は map の**状態機械**である。

### map の実装(crystal = mode 2)で確定したこと

1. **`quantum:crystal/tick` は `state` で分岐する**:
   `unless state 1..2 → bin/13`(PASSIVE = `crystal/passive/main`)、`if state 1..2 → bin/27`(ATTACK =
   アンカー連鎖 + クリスタル + 剣 + 追いパール)。
   アンカー/クリスタルは**ATTACK のときだけ**動き、PASSIVE では 1 本も撃たない。
2. **`state` は `eval` から毎tick再計算**(`eval/biased`):
   `eval = (自分のHP − 相手のHP) × 20`(相手に再生効果があれば `+50`)
   `+ eval_hit × 80`(`can_hit` なら +2、不可なら −1。= `(botHP - targetHP) * 20` が支配項)。
   `eval <= -1` で **即 PASSIVE**、`bin/29` の 30 % ロールで攻撃継続。
   → 「HP で負けている間は引く」= 参照の周期の正体。
3. **run8 の 34 秒窓は純アンカー連鎖**: d≈3.3 固定・`hit=0 pc=0`・`place→charge→爆発→次place` が
   12 tick(0.60 s)ジャスト。この 34 秒で HP は 20.0 → 3.7(自分の爆風)。
4. 403 秒の内訳: ATTACK(アンカー/クリスタルのタイマが動いている)は **22.5 %**、
   PASSIVE が **77.5 %**。アンカー設置は 403 秒で 117 回 = 17.4/min(既存の 420 秒表 18.1 と一致)。
5. `xaniclelib:anchor/explosion` は**バニラ爆発ではない**: `setblock ~ ~ ~ air` + `swing once` +
   `quantum:bad_respawn_point` エンチャント付きレンガを持った armor_stand を出すだけで、
   **島のデータパック内に `damage` コマンドが1つも無い**(`xaniclelib/anchor/` 全ファイル)。
   実際の爆風(ダメージ/ノックバック)は**練習MOD(HEhero)側**が `explosionNoBlockDamage` 設定で出している。
   = 参照の「1連鎖あたり自分が受けるダメージ」は MOD 依存で、バニラ power 5 をそのまま当てると
   こちらのBOTは 1 連鎖で 4〜7 HP 持っていかれる(参照は正味 0.3 HP/連鎖)。ここの較正が次の課題。

### プラグイン側の推移(石100床・通常戦・INTERMEDIATE・145秒)

```
                    参照   b16    b17    b18    b19    b20    b22=現行  b25/b26(状態機械)
滞在 <=2            4.6%   2.9    15.1   3.7    2.3    5.3     4.6       11.9
滞在 <=3            3.5%   4.7    12.3   5.2    2.2    5.6     4.3        3.2
滞在 <=6           37.3%  17.8    21.4  33.2   16.8   24.2    21.9        8.5
滞在 <=9            2.5%  24.2     1.2   7.7    8.3    7.8     3.6        2.7
滞在 >9            52.1%  50.4    49.9  50.1   70.3   57.1    65.6       73.6
pearl /min         49.2   55.9    59.6  57.1   41.0   50.1    38.9       38.2
anchor place /min  18.1   48.4    44.3  31.4   15.7   22.8    23.2        4.1
crystal place /min  5.3   21.9    12.4   5.0    0.4   11.2     6.6       11.8
melee swing /min   11.4    9.1    30.5  27.7   10.3   18.2    19.0        9.4
```

- b22(現行コミット)の詳細: アンカー 56/52/52、クリスタル 16/16、パール 94、剣 46/46、gap 1。
  滞在 <=6 が 21.9 % と参照の 6 割で、パールが 38.9/min(参照 49.2)と少ない。
  → **`PASSIVE_ESCAPE_WINDOW_MS = 2000`(逃げパール後の 2 秒徒歩窓)が効果を打ち消している**。
  b18(この窓なし)では滞在 <=6 33.2 % / パール 57.1/min まで来ていた。
- b25/b26 は map 準拠の**状態機械**(上記 1〜3)を実装して測ったもの。構造は出た
  (引く= >9 滞在 73.6 %、近接 9.4/min は参照並み)が、**自爆ダメージがバニラ値のまま**なので
  BOT が 100 秒でトーテム 3 回 POP(参照は 403 秒で 3 回)して試合が終わり、
  ATTACK 中のアンカーが 4.1/min まで落ちた。MOD 依存の爆風を較正してから入れ直す。

### 次にやること(優先順)

1. `PASSIVE_ESCAPE_WINDOW_MS` を 0 に戻す(または状態機械の PASSIVE 分岐に置き換える)。
   b18 の実測がこの設定の上限値(<=6 33.2 % / パール 57.1 / クリスタル 5.0 ✓)。
2. 状態機械を本実装: `state`/`eval` を `PracticeSession.BotAbilityState` に持ち、
   `eval = (botHP - targetHP) * 20 + (相手が再生中なら +50) + (attack中: can_hit ? +160 : -80)`、
   `eval <= -1` で PASSIVE(`bin/13`)= 逃げパール(<=8)/gap/クロスボウ/block のみ、
   ATTACK(`bin/27`)= アンカー連鎖 + クリスタル + 剣 + 追いパール。
3. **自爆ダメージの較正**: 参照の 1 連鎖あたり正味 0.3 HP に合わせる(MOD の爆風相当)。
   ハーネス側も参照と同じ `resistance 2 / regeneration 2 3` で延命する
   (現在は「HP 4 以下で全回復」= 相手 HP が常に 20 になり、BOT の `eval` が常に正になって
   PASSIVE に入れない。b25 実測: 97.8 % が 3-6 blk で連鎖しっぱなし、パール 2 回/試合)。
4. アンカー設置は 3-6 blk(参照: <=2 21 % / <=3 10 % / 3-6 69 %)。現行は <=2 61 % / <=3 28 %。

## 2026-09-15 (3): 状態機械を入れて測り直した結果(b1〜b6)

> 注: サンドボックスが再起動して `/tmp` が消えたため、ビルド番号は **b1 から振り直し**。
> 下の b1〜b6 は前節までの b1〜b26 とは別系列。

再開手順(すべて `/tmp` は配信ブランチから復元した):
`refs/delivery/java-env-delivery` → `/tmp/toolchain/jdk-21.0.12.1+1`、
`refs/delivery/paper-server-delivery` → `/tmp/paper-run`、
`refs/delivery/plugin-delivery` → `/tmp/base_ci.jar`(1.76.22 の CI jar に変更クラスを被せる)。

### 入れたもの(b1〜b6、いずれも `PracticeService`)

| # | 内容 | 実測 |
|---|---|---|
| b1 | map 準拠の eval/state 機械 + PASSIVE 分岐 + 裸の偽プレイヤーには map の botgear/neth キット | アンカー 145 回/145 s、**自分の爆風で HP が 1 も減らない**(HP 20.0 固定)。バニラの `Level.explode` は「爆発の発生源=自分」を除外するため |
| b2/b3 | 自爆ダメージを明示的に与える(`SELF_BLAST_RAW 1.6`、k² 減衰、アンカー power 5 / クリスタル power 6) | 自爆は乗った(1連鎖 0.3〜0.7 HP、参照 run8 の 0.68 と同帯)が、**PASSIVE の片道切符**: フル HP・距離 ≤15 でも eval が −30 になり二度と戻れない(144 s で遷移 1 回、以後 87 s はパールだけ) |
| b4 | eval の +225 / hit ±80 を **ATTACK 中のみ**適用、クリスタルの gap 上限撤廃、チェイスパールを連鎖中も許可、パール内側ゲート撤去、遠距離は PASSIVE からパールで入る | パール 43.0/min(参照 49.2)、滞在 ≤6 が 63.7 % まで回復。ただし **遷移が 57 回も往復**(eval −2 ⇄ −147)し、HP 12 で 87 s 停滞。crystal 0 / melee 0(下の standoff 3.0 が 3.5/3.0 の射程を締め出していた) |
| b5 | リンゴ在庫 2 → 64 + 補充(回復で eval が戻る)、`FIGHT_STANDOFF_NEAR 3.0 → 1.5`、`PEARL_PRESSURE_STANDOFF 4.0 → 3.0`、自爆 1.6 → 2.1 | **アンカー連鎖が再び機械化**(53.4/min、滞在 ≤6 99.4 %、パール 0)。HP は 19.0 で安定し eval が常に正 = state 1 固定 |
| b6 | **逃走パールを state 分岐の上へ移動**(map `bin/13` は `crystal/passive/main` を先に呼び、`if state 1..2 return 0` を後ろに置く = パール離脱は PASSIVE 専用ではない) | 滞在 >9 が 84.8 % まで跳ねた(参照 52.1 %)。パール 29.4/min、アンカー 4.6/min、クリスタル 1.2、剣 0.8 = 今度は遠すぎて何もできない |

### 分かったこと

1. **eval の +225 は「ATTACK 中のみ」**。`eval/tick` は `if score @s state matches 1..2` の下で加算し、
   その直後の `eval/biased` が state を決める。したがって **PASSIVE に入る閾値は
   `(botHP − targetHP) × 20 + 50 + hit ≤ −226`**、つまり 20 HP の相手なら自 HP 6.25 以下。
   逆に `bin/29` のロールは `eval ∈ −224..−1` の窓でしか戻せないので、
   **遠距離(ターゲットが 15 ブロック以遠)で +225 が消えることも PASSIVE の主要因**。
2. **逃走パールは分岐の外**。`bin/13` の構造上、ATTACK 中でも 8 ブロック以内ならパールで離脱する。
   これが参照の「median 18.3 ブロック / 52 % が >9」という距離分布の正体。
   プラグインで PASSIVE の中だけに置いていたのが、3〜6 ブロックでアンカーを打ち続ける原因だった。
3. 参照の距離分布(run8、静止ターゲット再構成): `<=2 4.5 / <=3 12.2 / <=6 36.8 / <=9 41.3 / >9 58.7 %`。
   近接と遠距離の**両方**に十分な時間を使うのが参照のリズムで、こちらはどちらかの端に張り付きやすい。
4. 自爆ダメージは `SELF_BLAST_RAW 2.1`(k² 減衰)で参照の 0.68 HP/連鎖にほぼ一致する見込み
   (b5 で HP 19.0 平均 = 回復と釣り合っている)。

### いまの状態

- `gradle.properties` = 1.76.24、作業ツリーに b1〜b6 の変更(未コミット)。
- **未達**: 滞在バンド(>9 が過剰)、パール 29.4/min(参照 49.2)、アンカー 4.6/min(参照 18.1)。
  b6 は「入ってくる動き」が弱い。次は (a) 逃走パールの反応遅延(直後に投げ返さない)、
  (b) `bin/28` の `move forward` を 10 ブロック以遠でより強く/継続的に、
  (c) チェイスパールの着地点(`PEARL_PRESSURE_STANDOFF`)の再調整、の順で試す。
- 会場・レシピは不変: `room r1 CRYSTAL 40 0 64 0` → `dummy HarnessBot 4 64 4` → `ground 40 100` →
  `fight CRYSTAL 150 INTERMEDIATE 1`。

## 2026-09-15 (4): b7〜b25 — パール着地の修正とチェイスパールの復活

会場・手順は b1〜b6 と同一(`room r1 CRYSTAL 40 0 64 0` → `dummy HarnessBot 4 64 4` →
`ground 40 100` → `fight CRYSTAL 150 INTERMEDIATE 1`)。1 世界 1 試合に限定し、
`tools/fight_profile.py plugin <log>` で参照バンドと突き合わせる。

### 診断の連鎖

| # | 変更 | 実測 |
|---|---|---|
| b7 | 逃走パールの帯上限 10.0 → 8.0(map `bin/13` の `distance=..8` = チェイス側ゲートと同じ縁) | 滞在 ≤6 が 8.8 → 13.0 %、>9 84.8 → 73.0 %。ただし out/in 振動: 逃走 +1000 の再アームが 1 回/s、チェイス着地は `-3`(地面はあるが立ち位置が無い)で失敗 |
| b8〜b11 | 着地探索の失敗理由を計測(タグ `chase d=`、`[pearl] set`、失敗コード) | チェイス着地が 145 s で **34 回失敗**。失敗のたびに共有 `pearlcd` を +250 ms 押し戻すためチェイスパールが飢餓状態になり、戻りが 3 s の徒歩になっていた(徒歩が 6-9 帯 16.6 % の正体) |
| b13 | `findPearlLanding` を書き直し: レイに沿って最初の固体地面まで降り、そこから k=1..6 まで登って「2 ブロック空いた場所」に立つ。試行距離に `preferred*0.55/0.35` を追加。領域判定は非 static の `inside()` に集約 | 着地失敗 0 件、`in +1000` ×11、`out +1000` ×12。滞在 `0.0/2.5/50.1/16.6/30.8`、アンカー 14.1/min ✓、剣 8.3/min ✓、クリスタル 13.7/min ✗、パール 37.7/min ✗ |
| b14〜b17 | ハーネス側の確認: ダミーは**部屋のキットを着たプレイヤー**(ARMOR 属性 20 + resistance I)で、`/damage` や NBT を**ラウンド間**に叩くとロビー状態(resistance 255・ワールドスポーン y=113)を読んでしまい「無敵のダミー」に見えていた。ハーネスは `dummy` 生成時とラウンド開始時に `GameMode.SURVIVAL` を明示 | 試合中は参照と同じ「硬いが削れる相手」。DRAW でも `pops=1` が出る試合あり |
| b18 | **パッシブを gap_timer(リンゴ)ゲートの上へ移動**。map の `crystal/tick` は `holeoffense` と `crystal/passive/main`(逃走パール)を戦闘ループより上で呼ぶので、咀嚼中でも離脱できる | 咀嚼 1 回 1.75 s × 2 の「3-4 ブロックで 4 s 立ち尽くし」が消えた。b18(移動前)は ≤6 50.0 %>9 28.4 %、b19(移動後)は ≤6 32.8 %>9 57.7 %(参照 45.4/52.1) |
| b20〜b23 | チェイス着地点(`PEARL_PRESSURE_STANDOFF`)の掃引: 8 → 6-9 帯に 19.3 % の徒歩を作り ✗、4 → 3-6 帯 47 % ✗、3.4 → 44 % ✗ | 3.0 が最良(3-6 帯 38.3 %、参照 37.3) |
| b24 | クリスタルを**アンカー連鎖 3 回につき 1 回**に制限(参照の武器比 18.1:5.3 = 1:3.4 を再現。それまでのゲートは「射程 3.5 + cd + アンカー段が空」だけで、アンカー間隙ごとに発火していた) | クリスタル 13.7 → 5.0/min ✓、剣 8.3/min ✓、パール 55.4/min ✓ |

### b24(= 1.76.25 としてコミット)の突き合わせ

| 指標 | 当プラグイン | 参照 | 判定(±10 %) |
|---|---|---|---|
| 滞在 ≤2 / (2,3] / (3,6] / (6,9] / >9 | 0.0 / 6.3 / 38.3 / 1.9 / 53.5 % | 4.6 / 3.5 / 37.3 / 2.5 / 52.1 % | (3,6] ✅ / >9 ✅ / (6,9] ほぼ ✅(1.9)/ ≤2・(2,3] ❌ |
| パール投擲 | 55.4/min | 49.2/min(許容 48-56) | ✅ |
| アンカー設置 | 22.3/min | 18.1/min(許容 10.5-17.4) | ❌(+23 %) |
| クリスタル設置 | 5.0/min | 5.3/min(許容 4.5-6.6) | ✅ |
| 剣 swing = hit | 8.3/min | 11.4/min(許容 7.9-11.4) | ✅ |

### 残差と次のレバー

1. **≤2 と (2,3] の配分**(参照 4.6/3.5、当側 0.0/6.3)。合計 ≤3 は 6.3 % で参照 8.1 % に近いが、
   当側はチェイス着地が「3 ブロック」固定なので 2-3 帯に溜まる。着地を 2 ブロックにすると
   ≤2 が立ち、代わりに (3,6] が薄くなるため、着地距離と `FIGHT_STANDOFF_NEAR` の同時調整が要る。
2. **6-9 帯 1.9 %(参照 2.5 %)**。ほぼ一致だが、逃げ→チェイスの 1 秒窓で徒歩が入ると 19 % に跳ねる
   (b21)。チェイスの着地点を固定せず「射線が当たった場所」にする余地あり。
3. **アンカー設置 22.3/min(参照 18.1)**。1 連鎖 0.6 s の burst 構造は合っているので、
   burst の間にどれだけ帯から出るかの差。参照の 3.3 s 間隔に合わせるにはアンカー cd ではなく
   「帯から出る時間」を伸ばす(チェイス着地とパール間隔の連動)。
4. 自爆の HP 収支は参照帯(1 連鎖 0.3〜0.7 HP)を維持。`pops=0` の DRAW が続くのは
   ダミーが部屋のキット(ARMOR 20 + resistance I)を着ているためで、試合によっては `pops=1` が出る。

## 2026-09-15 (5): b28/b29 の検証、全キット突き合わせ、Paper 版 HeroBot の実現性

### b28 / b29 と、その差し戻し

| build | 変更 | 実測(CRYSTAL / INTERMEDIATE / 100 床 / 通常戦) | 判定 |
|---|---|---|---|
| b28 | ソース同一の再ビルド(挙動不変) | 5 モード連続チェック用に使用(下記) | 基準として採用 |
| b29 | `tickPearlPressure` に「アンカー段が動いている間は発射しない」ゲート(map の `tag=hardcode` マーカー相当) | アンカー設置 62 回 / 26.6 min⁻¹、連鎖中断 27/62(44 %)、クリスタル 4.3/min、剣 12.9/min、パール 51.0/min | **差し戻し** |

- b29 の狙いは「b25 の 58 設置中 23 中断は、アンカー段の途中でチェイスパールが発射されて連鎖が切れるから」という
  仮説の検証だった。ゲート自体は入ったが、**中断率は改善しなかった**(b24 16/54 = 30 %、b25 23/58 = 40 %、b29 27/62 = 44 %)。
- 途中パールの検出器(直前サンプルが `GLOWSTONE`/`RESPAWN_ANCHOR` → 次が `ENDER_PEARL` かつ 3 ブロック超の移動)では
  b29 17 件/140 s、b25 11 件/145 s、b24 11 件/145 s と**むしろ増えた**。跳躍距離はすべて 14.4〜15.7 ブロック =
  `PASSIVE_PEARL_LEAP` の逃走パールで、`tickPearlPressure`(チェイスパール)ではない。つまり
  **アンカー連鎖を切っているのは逃走パール側**で、チェイスパールを止めるゲートは効かない(仮説は部分的に棄却)。
- よって b29 は採用せず `PracticeService` は e24de91 の状態(= b24/b25 のソース)へ戻した。`gradle test shadowJar` もこの状態で通る。

### 全キットチェック(b28、1 回の起動で 5 モード連続、すべて DRAW・ダミー HP 20 → 20)

| モード | 滞在 ≤2 / (2,3] / (3,6] / (6,9] / >9 | 剣 hit | ダミー与ダメ | 備考 |
|---|---|---|---|---|
| CRYSTAL | 0.0 / 11.9 / 35.3 / 0.7 / 52.1 % | 12 | 96 | アンカー 37p/28c/21d(19.3/min)、パール out 56 / in 55、クリスタル 6p/6d |
| SWORD | 3.9 / 70.1 / 3.6 / 2.1 / 20.3 % | 103 | 900 | 接近して剣を振り続ける(モード通り) |
| NETHERITE_POT | 2.8 / 71.1 / 4.7 / 2.1 / 19.3 % | 107 | 922 | 同上 |
| MACE | (サンプル 0) | 133 | 1064 | トレースが 1 件も出ない = 計測側の穴(要修正) |
| CART | 0.0 / 0.0 / 0.0 / 99.1 / 0.9 % | – | – | **異常: 手持ちが `END_CRYSTAL` 886 / `BOW` 264 サンプル。`POWERED_RAIL`/`TNT_MINECART` が一度も出ない** |

- SWORD / NETHERITE_POT は 70 % が (2,3] 帯 = 密着して剣を振る。CRYSTAL だけが遠距離 52 % という別物の挙動で、
  モードごとの性格は出ている。
- CART は「トロッコ用の装備を着ていない」= モード → キットの紐づけ(`/botadmin`)が効いていない可能性が高い。
  次の計測で `/botadmin CART <kit>` を明示して再取得する。
- MACE は攻撃は通っている(1064 ダメージ)が trace が出ない。ハーネス側の trace 出力条件を確認する。

### 参照側(Quantum)のキット実体 — `/bot` の中身から

`Quantum's PvP Practice v1.18.zip` の datapack を展開して確認した。**QBOT が着る装備は kit1〜15 のチェストではなく
`quantum:botgear/neth|dia`** が直接 `item replace` で作る(`options/<mode>` から `.gear toggles` で選択)。

| スロット | botgear/neth(既定) | crystal(mode 2)の差分 |
|---|---|---|
| armor | netherite protection4/blast_protection4/feather_falling4 unbreakable | legs と boots を blast_protection4 に差し替え |
| offhand | totem_of_undying | – |
| hotbar 0 | totem(knockback 1) | – |
| hotbar 1 | obsidian ×64 | – |
| hotbar 2 | end_crystal ×64 | – |
| hotbar 3 | netherite_sword sharpness5(+knockback1) | sweeping_edge3 版に差し替え |
| hotbar 4 | golden_apple ×64 | shield(unbreakable) |
| hotbar 5 | netherite_axe sharpness5 | – |
| hotbar 6 | ender_pearl ×16 | – |
| hotbar 7 | respawn_anchor ×64 | – |
| hotbar 8 | glowstone ×64 | – |
| inventory 0/1 | tipped_arrow(strong_harming ×99) | long_slow_falling ×99 |
| inventory 2/3/4,7 | water_bucket / lava_bucket | – |

- モード番号: `options/sword=1, crystal=2, mace=3, nethpot=4, pot=5, cart=6`。パリティ計測は `difficulty/2 = INTERMEDIATE`(docs の記録と一致)。
- kit1〜15 はチェスト実体(`positioned <x> 78 <z>` + `block ~ ~ ~-1 container.N`)で、kit1(ネザライト+エリトラ+クロスボウ)、
  kit2(ネザライト+クロスボウ+トーテム99)、kit3(ダイヤ+盾+弓)、kit4(メイス/パール/エリトラ)、kit5(ポーション各種)まで実機で読めた。
  kit6〜9・13 は指定座標がチェストでなかった(壁の向きが別)ため未取得 — 本体側を動かす今回の方針では不要。
- 当プラグインの `stockBotInventory` は上表の botgear/neth をモード別に写しており、アンカー 16・パール 64 など
  **個数だけ**参照と違う(参照はアンカー 64・パール 16)。個数は自動補給で吸収されるため挙動差にはならない。

### 「Paper 版 HeroBot + 戦闘関数コピペ」の実現性調査(実験済み)

方針そのものは正しい(定数合わせをやめて Quantum の関数をそのまま走らせる)。ただし **Paper では datapack 関数から
プラグインコマンドを呼べない**という決定的な障害があることを実測で確認した。

実験(Paper 1.21.11-132 + 検証用プラグイン + `world/datapacks/spike`):

| 試行 | 結果 |
|---|---|
| Bukkit `plugin.yml` のコマンド(`player`/`playerspawn`/`herobot`) | 起動時の関数パースで `Unknown or incomplete command`(プラグイン有効化は datapack パースの**後**) |
| `MinecraftServer#getCommands().getDispatcher()` へ直接登録(NMS) | 登録はできるが Paper のコマンド同期で消え、`/player` すら Unknown |
| Paper Brigadier API(`LifecycleEvents.COMMANDS`)で登録 | **コンソール/RCON からは herobot と完全同一の引数文字列で動く**(`player @s stop`、`playerspawn X at 0 65 0 facing 0 0 in survival on minecraft:overworld`、`herobot … perm world` がすべてハンドラに到達) |
| `Server#reloadData()` | レシピ等は再読込するが**関数は再パースしない**(関数は Unknown のまま) |
| `/reload` | 関数は再パースされるが、その瞬間プラグインは無効化されているため `player` は Unknown のまま |

→ 結論: **datapack ローダーに任せる限り `player` 系の行は絶対にパースできない**。回避策は
「関数ファイルを自前で読んで、生きている dispatcher で 1 行ずつパースし、`player`/`playerspawn`/`herobot`/`function` だけ
自前実装に横取りする関数エンジン」をプラグイン側に持つこと。バニラコマンド(`scoreboard`/`data`/`execute`/`item`/`tp`/`tag`…)
はそのまま dispatcher に流せるので、実装量は「動詞ブリッジ + 約 200 行のインタプリタ」で済む。

移植に必要なもの(調査で確定):

1. **動詞**: `player @s stop | sprint | move [forward|backward|left|right] | jump | hotbar <n> | use once | attack once`、
   `playerspawn <name> at <x> <y> <z> facing <yaw> <pitch> in <gamemode> on <dimension>`、
   `herobot <option> <value> perm <world>`(`explosionNoBlockDamage`/`explosionNoFire`/`shieldStunning` 等)。
   いずれも Paper Brigadier API で同名登録でき、文字列はそのまま受け取れることは実測済み。
2. **エンジン**: `.mcfunction` の行パース、`$` マクロ、`return`/`return run`、`function <ns>:<name>` の再帰呼び出し、
   毎 tick の `#minecraft:tick` 相当の起動。ここを自前で持てば datapack ローダーに依存しない。
3. **世界**: map 側のアリーナ・キットチェスト・`positioned over world_surface` などは座標依存。当側の石 100 床アリーナで
   走らせるなら、アンカー/クリスタルの usable 判定に使うマーカー系はそのまま動くが、`kits/*`(チェスト)は使えない。
4. **擬似プレイヤー**: `player … move forward` は herobot が「入力フラグ」を立てる実装。当側は既に
   PacketBot/ServerPlayer を直接動かしているので、同じ動詞に写せる(実装は別途)。
5. **リスク**: 20 Hz で 1000 ファイル規模の関数を回す性能、`@p[tag=xlib_target]` 前提の相手解決、死亡/リセット処理。

寄り道として、この構成なら **QBOT 対 QBOT** も自然に作れる(map 側が `as quantumbot` で 1 体しか駆動しない問題は、
ブリッジ側で 2 体ぶん `npc:tick` を呼び、タグを交互に付け替えればよい。当側は 1 tick 内で相手タグを入れ替える
`quantum:ref_harness/twobot` を用意して実験した — 実機ログは未取得)。

### 次の一手(優先順)

1. **関数エンジン + 動詞ブリッジの PoC**: `crystal/tick` と `g1gc/*` だけでよいので、1 体の擬似プレイヤーで
   map の関数をそのまま動かす。動いたら「定数合わせ」を全部捨てて、同じ計測(`tools/fight_profile.py`)で突き合わせる。
2. 既存の手書き BOT(`tickCrystalBot` 系)はフォールバックとして残し、切り替えは `/botadmin` 相当のフラグで。
3. CART モードのキット紐づけと MACE の trace 欠落を直してから 5 モード再計測。
